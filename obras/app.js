function bootObras(){
  'use strict';
  const APP='obras',THEME_KEY='321modular_theme';
  const STATUS=[
    {id:'planejamento',label:'Planejamento',color:'#7C5CBF'},
    {id:'aguardando',label:'Aguardando início',color:'#d6a620'},
    {id:'em_andamento',label:'Em andamento',color:'#2d5da9'},
    {id:'pausada',label:'Pausada',color:'#c94b3f'},
    {id:'manutencoes',label:'Manutenções',color:'#f9b215'},
    {id:'concluida',label:'Concluída',color:'#2B8A3E'}
  ];
  const DEFAULT_CHECKLIST_GROUPS=[
    {title:'ETAPA 1 - PROGRAMAÇÃO DE MONTAGEM',items:[
      'Terreno Liberado - terreno livre e sem restrições para o início das atividades de montagem da obra (instalações de blocos e da construção).',
      'Agendamento da entrega com a Fábrica','Agendamento com equipe de mão de obra','Agendamento com serviços de munck','Pedido das telhas e acessórios','Pedidos adicionais para serviços complementares'
    ]},
    {title:'ETAPA 2 - MONTAGEM',items:[
      'Conferência e Revisão do Manual de Montagem - Receber o manual de montagem da obra, revisá-lo, sanando toda e qualquer dúvida relacionada ao processo de montagem da obra.',
      'Instalação Alicerce - blocos instalados e finalizados para receber as estruturas em madeira.',
      'Instalação Estrutura - paredes, coberturas e telhas instalados.',
      'Instalação Acabamento - vistas, meia cana, rodapés, rejuntes de banheiros.',
      'Aberturas - regulagem de todas as portas e janelas da construção.',
      'Checklist Interno - revisar criteriosamente: 01. Junção de pisos; 02. Acabamentos de meia cana, rodapés e vistas; 03. Perfeito funcionamento das portas e janelas; 04. Revisar o rejunte dos laminados de fibra de vidro; 05. Revisar desnível do assoalho no piso do box do banheiro; 06. Revisar instalações das telhas e cumeeiras, parafusos e amassados; 07. Limpeza da obra e do entorno.',
      'Destinação correta dos resíduos de obra - descartar corretamente os resíduos gerados na obra, separando as madeiras que poderão ser reaproveitadas, das que devem ser descartadas, e também os plásticos e outras sobras que não podem ter o mesmo destino das madeiras.',
      'Checklist Externo - realizar juntamente com o cliente o checklist da obra, revisando todos os itens do checklist interno. Esclarecer possíveis dúvidas e orientar quanto aos cuidados de manutenção da obra.',
      'Álbum de fotos - registrar fotos da obra finalizada, com destaque aos itens relacionados ao checklist interno.'
    ]},
    {title:'ETAPA 3 - FINALIZADO',items:['Checklist e entrega realizados','Recebimento (R$) finalizados','Álbum de fotos','Revisão da obra com time envolvido','Apresentação dos resultados']},
    {title:'ETAPA 4 - MANUTENÇÃO',items:['Agendar visita','Agendar manutenção','Registrar manutenções realizadas']}
  ];
  const PRIORITY={baixa:'#2B8A3E',media:'#d6a620',alta:'#E8590C',urgente:'#c0392b'};
  const VIEW_META={painel:['Painel','Acompanhe cada etapa das obras da franquia.'],calendario:['Calendário de obras','Visualize as datas previstas das obras por mês.'],cronograma:['Cronograma','Acompanhe obras, etapas e atividades em uma linha do tempo.'],locais:['Locais das obras','Navegue pelo mapa completo das obras cadastradas.']};
  const $=id=>document.getElementById(id);
  const state={client:null,profile:null,franchises:[],carpenters:[],works:[],locations:{states:[],cities:[]},view:'painel',query:'',franchise:'',calendar:new Date(),timelineAnchor:new Date(),timelineInitialized:false,timelineCollapsed:new Set(),timelineGroupCollapsed:new Set(),timelineSaving:false,timelineAutoCenter:true,timelineUndo:[],timelineRedo:[],timelineHistoryBusy:false,confirmResolver:null,sort:'recent',hideOldCompleted:true,visibleStatuses:new Set(STATUS.map(item=>item.id)),editing:null,dragged:null,map:null,mapLayer:null,mapBaseLayers:null,mapMarkers:new Map(),mapHasFit:false,datePicker:null,loadingWorks:false,loadError:false};
  const esc=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const statusBy=id=>STATUS.find(item=>item.id===id)||STATUS[0];
  const dateLabel=value=>value?new Intl.DateTimeFormat('pt-BR').format(new Date(value+'T12:00:00')):'Sem data';
  const titleCase=value=>String(value||'').replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
  function toast(message,type){const el=$('toast');el.textContent=message;el.className='toast show '+(type||'');clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.className='toast',3200)}
  function safeMessage(error,fallback){console.error('[obras]',error);return window.SuperAppAuth?.getSafeAuthMessage?.(error,fallback)||fallback}
  async function rpc(name,params){const {data,error}=await state.client.rpc(name,params||{});if(error)throw error;return data}
  function withTimeout(promise,timeoutMs,label){let timer;return Promise.race([promise,new Promise((_,reject)=>{timer=setTimeout(()=>reject(new Error(label||'Tempo limite excedido')),timeoutMs)})]).finally(()=>clearTimeout(timer))}
  function rows(value){if(Array.isArray(value))return value;if(Array.isArray(value?.items))return value.items;return value?[value]:[]}
  function isMFrq(){return state.profile?.role_code==='MFrq'}
  function workVisibleToCurrentRole(work){return !isMFrq()||work?.status==='em_andamento'}
  function canCreateWork(){return ['ADM','GEng','GFrq','OFrq'].includes(state.profile?.role_code)}
  function canEditWork(){return ['ADM','GEng','GFrq','OFrq'].includes(state.profile?.role_code)}
  function canDeleteWork(){return ['ADM','GFrq','OFrq'].includes(state.profile?.role_code)}
  function availableStatuses(){return isMFrq()?STATUS.filter(status=>status.id==='em_andamento'):STATUS}
  function filtered(){const q=state.query.trim().toLocaleLowerCase('pt-BR'),cutoff=Date.now()-30*86400000;return state.works.filter(work=>workVisibleToCurrentRole(work)&&(!state.franchise||work.franchise_id===state.franchise)&&state.visibleStatuses.has(work.status)&&(!state.hideOldCompleted||work.status!=='concluida'||!work.completed_at||new Date(work.completed_at).getTime()>=cutoff)&&(!q||[work.title,work.city,work.state,work.responsible_name,work.franchise_name,work.code].some(v=>String(v||'').toLocaleLowerCase('pt-BR').includes(q))))}
  function mapFiltered(){return state.works.filter(work=>workVisibleToCurrentRole(work)&&(!state.franchise||work.franchise_id===state.franchise))}
  function coordinateNumber(value){if(value===null||value===undefined||value==='')return null;const parsed=Number(String(value).trim().replace(',','.'));return Number.isFinite(parsed)?parsed:null}
  function workCoordinates(work){const latitude=coordinateNumber(work?.latitude??work?.lat),longitude=coordinateNumber(work?.longitude??work?.lng??work?.lon);return latitude!==null&&longitude!==null&&Math.abs(latitude)<=90&&Math.abs(longitude)<=180?{latitude,longitude}:null}
  function googleMapUrl(work){const coords=workCoordinates(work);if(!coords)return 'https://www.google.com/maps';const query=coords.latitude.toFixed(6)+','+coords.longitude.toFixed(6);return 'https://www.google.com/maps/search/?api=1&query='+encodeURIComponent(query)}
  function sortWorks(items){const priority={urgente:4,alta:3,media:2,baixa:1};return [...items].sort((a,b)=>state.sort==='oldest'?new Date(a.created_at)-new Date(b.created_at):state.sort==='start'?String(a.start_date||'9999').localeCompare(String(b.start_date||'9999')):state.sort==='priority'?(priority[b.priority]||0)-(priority[a.priority]||0):state.sort==='name'?String(a.title).localeCompare(String(b.title),'pt-BR'):new Date(b.created_at)-new Date(a.created_at))}
  function setBusy(busy){$('app-shell').setAttribute('aria-busy',String(Boolean(busy)));$('refresh-btn').disabled=busy;$('save-btn').disabled=busy}
  function configureTheme(){const button=$('theme-toggle');function sync(){button.querySelector('.knob').textContent=document.documentElement.dataset.theme==='dark'?'🌙':'☀️';if(state.map)setTimeout(()=>state.map.invalidateSize(),20)}sync();button.addEventListener('click',()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;localStorage.setItem(THEME_KEY,next);sync()})}
  function configureShell(){
    document.querySelectorAll('[data-view]').forEach(btn=>btn.addEventListener('click',()=>showView(btn.dataset.view)));
    $('hamburger-btn').addEventListener('click',()=>toggleAside(true));$('aside-backdrop').addEventListener('click',()=>toggleAside(false));
    $('exit-btn').addEventListener('click',()=>location.href=SuperAppAuth.getPortalUrl());$('refresh-btn').addEventListener('click',loadWorks);$('new-work-btn').addEventListener('click',()=>openModal());
    $('search-input').addEventListener('input',e=>{state.query=e.target.value;render()});$('franchise-filter').addEventListener('change',e=>{state.franchise=e.target.value;state.mapHasFit=false;render()});
    $('sort-filter').addEventListener('change',e=>{state.sort=e.target.value;render()});$('hide-old-completed').addEventListener('change',e=>{state.hideOldCompleted=e.target.checked;render()});
    $('modal-close').addEventListener('click',closeModal);$('cancel-btn').addEventListener('click',closeModal);$('modal-backdrop').addEventListener('click',e=>{if(e.target===$('modal-backdrop'))closeModal()});
    $('work-form').addEventListener('submit',saveWork);$('delete-btn').addEventListener('click',deleteWork);
    $('calendar-prev').addEventListener('click',()=>shiftMonth(-1));$('calendar-next').addEventListener('click',()=>shiftMonth(1));$('calendar-today').addEventListener('click',()=>{state.calendar=new Date();renderCalendar()});
    $('timeline-prev').addEventListener('click',()=>shiftTimelineMonth(-1));$('timeline-next').addEventListener('click',()=>shiftTimelineMonth(1));$('timeline-today').addEventListener('click',()=>goToTimelineDate(new Date(),true));
    document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeFloatingMenus();if($('modal-backdrop').classList.contains('open'))closeModal()}});$('status-filter').addEventListener('toggle',()=>{if($('status-filter').open){closeCustomSelects();closeDatePickers()}});
    renderStatusFilters();enhanceSelects(document);
  }
  function toggleAside(open){$('aside').classList.toggle('open',open);$('aside-backdrop').classList.toggle('open',open);document.documentElement.classList.toggle('menu-open',open);document.body.classList.toggle('menu-open',open);$('hamburger-btn').setAttribute('aria-expanded',String(open));$('hamburger-btn').setAttribute('aria-label',open?'Fechar menu':'Abrir menu');if(!open&&state.map)setTimeout(()=>state.map.invalidateSize(),220)}
  function showView(view){state.view=view;document.querySelectorAll('[data-view]').forEach(btn=>btn.classList.toggle('active',btn.dataset.view===view));document.querySelectorAll('.view').forEach(el=>el.classList.toggle('active',el.id==='view-'+view));$('page-title').textContent=VIEW_META[view][0];$('page-subtitle').textContent=VIEW_META[view][1];$('new-work-btn').hidden=view!=='painel'||!canCreateWork();toggleAside(false);render();if(view==='locais')setTimeout(()=>state.map?.invalidateSize(),80)}  function profileCopy(){const p=state.profile||{};$('side-user-name').textContent=p.display_name||'Usuário';$('side-user-role').textContent=(p.role_name||p.role_code||'Acesso operacional')+' · '+(p.franchise_name||p.unit_name||'acesso global');$('scope-badge').textContent=p.franchise_name?'Franquia · '+p.franchise_name:p.unit_name?'Matriz · '+p.unit_name:'Matriz · acesso global'}
  function renderFranchises(){const select=$('franchise-filter'),current=state.franchise;select.innerHTML='<option value="">Todas as franquias acessíveis</option>'+state.franchises.map(f=>'<option value="'+esc(f.id)+'">'+esc(f.display_name)+'</option>').join('');select.value=current;select.hidden=state.franchises.length<=1;if(state.franchises.length===1&&!state.franchise){state.franchise=state.franchises[0].id;select.value=state.franchise}refreshCustomSelect(select)}
  function renderStatusFilters(){const root=$('status-filter-options'),statuses=availableStatuses();$('status-filter').hidden=false;root.innerHTML=statuses.map(item=>'<label><input type="checkbox" value="'+esc(item.id)+'" checked><span style="--filter-color:'+item.color+'"></span>'+esc(item.label)+'</label>').join('');root.querySelectorAll('input').forEach(input=>input.addEventListener('change',()=>{input.checked?state.visibleStatuses.add(input.value):state.visibleStatuses.delete(input.value);render()}))}
  function closeFloatingMenus(){closeCustomSelects();closeDatePickers();if($('status-filter'))$('status-filter').open=false}
  function closeCustomSelects(except){document.querySelectorAll('.select-popover.open').forEach(panel=>{if(panel!==except)panel.classList.remove('open')});document.querySelectorAll('.custom-select-button.open').forEach(button=>{if(button._panel!==except)button.classList.remove('open')})}
  function positionCustomSelect(button,panel){const rect=button.getBoundingClientRect(),host=panel.closest('#timeline-jump-popover');if(host){const hostRect=host.getBoundingClientRect();panel.style.setProperty('--select-left',(rect.left-hostRect.left)+'px');panel.style.setProperty('--select-top',(rect.bottom-hostRect.top+6)+'px');panel.style.setProperty('--select-width',rect.width+'px');panel.style.maxHeight=Math.max(120,Math.min(250,window.innerHeight-rect.bottom-18))+'px';return}panel.style.left=rect.left+'px';panel.style.top=(rect.bottom+6)+'px';panel.style.width=Math.max(rect.width,220)+'px';const maxHeight=Math.min(300,window.innerHeight-rect.bottom-18);panel.style.maxHeight=Math.max(120,maxHeight)+'px'}
  function normalizeSelectSearch(value){return String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLocaleLowerCase('pt-BR').trim()}
  function refreshCustomSelect(select){
    if(!select?._customButton)return;
    const option=select.options[select.selectedIndex],searchable=select.id==='f-city';
    select._customButton.hidden=select.hidden;
    select._customButton.disabled=select.disabled;
    select._customButton.querySelector('span').textContent=option?.textContent||'Selecione';
    const optionsHtml=Array.from(select.options).map(opt=>'<button type="button" data-value="'+esc(opt.value)+'" class="'+(opt.selected?'selected':'')+'" '+(opt.disabled?'disabled':'')+'>'+esc(opt.textContent)+'</button>').join('');
    select._customPanel.innerHTML=(searchable?'<label class="select-search"><span class="sr-only">Buscar cidade</span><input type="search" autocomplete="off" placeholder="Digite para buscar a cidade…" aria-label="Buscar cidade"></label>':'')+'<div class="select-options">'+optionsHtml+'</div>'+(searchable?'<p class="select-search-empty" hidden>Nenhuma cidade encontrada.</p>':'');
    const optionButtons=[...select._customPanel.querySelectorAll('.select-options button')];
    optionButtons.forEach(optionButton=>optionButton.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();select.value=optionButton.dataset.value;select.dispatchEvent(new Event('change',{bubbles:true}));refreshCustomSelect(select);closeCustomSelects()}));
    if(searchable){
      const search=select._customPanel.querySelector('.select-search input'),empty=select._customPanel.querySelector('.select-search-empty');
      search.addEventListener('input',()=>{
        const query=normalizeSelectSearch(search.value);
        let visible=0;
        optionButtons.forEach(optionButton=>{
          const matches=!query||normalizeSelectSearch(optionButton.textContent).includes(query);
          optionButton.hidden=!matches;
          if(matches&&!optionButton.disabled)visible++;
        });
        empty.hidden=visible>0;
      });
    }
  }
  function enhanceSelect(select){if(!select||select.dataset.customSelect==='true')return refreshCustomSelect(select);select.dataset.customSelect='true';select.classList.add('native-select-hidden');const button=document.createElement('button'),panel=document.createElement('div');button.type='button';button.className='custom-select-button';button.innerHTML='<span></span><i aria-hidden="true"></i>';button.setAttribute('aria-label',select.getAttribute('aria-label')||select.closest('label')?.textContent?.trim()||'Selecionar opção');panel.className='select-popover';panel._select=select;const containedHost=select.closest('#timeline-jump-popover');if(containedHost){panel.classList.add('select-popover-contained');containedHost.appendChild(panel)}else document.body.appendChild(panel);select.insertAdjacentElement('afterend',button);select._customButton=button;select._customPanel=panel;button._panel=panel;button.addEventListener('click',()=>{const open=panel.classList.contains('open');if(!open)document.dispatchEvent(new CustomEvent('superapp:control-open',{detail:{trigger:button}}));closeDatePickers();if($('status-filter'))$('status-filter').open=false;closeCustomSelects(panel);panel.classList.toggle('open',!open);button.classList.toggle('open',!open);if(!open){positionCustomSelect(button,panel);requestAnimationFrame(()=>panel.querySelector('.select-search input')?.focus({preventScroll:true}))}});select.addEventListener('change',()=>refreshCustomSelect(select));refreshCustomSelect(select)}
  function enhanceSelects(root){root.querySelectorAll('select:not(.mobile-status)').forEach(enhanceSelect)}
  function repositionOpenControls(){document.querySelectorAll('.custom-select-button.open').forEach(button=>positionCustomSelect(button,button._panel));document.querySelectorAll('.app-date-field.open').forEach(field=>{if(field._panel)positionDatePicker(field,field._panel)})}
  window.addEventListener('resize',repositionOpenControls);document.addEventListener('scroll',repositionOpenControls,true);document.addEventListener('superapp:control-open',event=>{const trigger=event.detail?.trigger;if(!trigger?.classList?.contains('custom-select-button'))closeCustomSelects();if(!trigger?.closest?.('.app-date-field'))closeDatePickers();if($('status-filter')&&!trigger?.closest?.('#status-filter'))$('status-filter').open=false});document.addEventListener('click',event=>{if(!event.target.closest('.custom-select-button,.select-popover'))closeCustomSelects();if(!event.target.closest('.app-date-field,.date-popover'))closeDatePickers()});
  function isoToDisplay(value){if(!value)return '';const [y,m,d]=String(value).split('-');return y&&m&&d?d+'/'+m+'/'+y:''}
  function displayToIso(value){const match=String(value||'').match(/^(\d{2})\/(\d{2})\/(\d{4})$/);return match?match[3]+'-'+match[2]+'-'+match[1]:''}
  function dateFieldHtml(id,name,value,label,readOnly=false){return '<div class="app-date-field'+(readOnly?' is-readonly':'')+'" data-date-field'+(readOnly?' data-readonly="true"':'')+'><input class="date-display" id="'+esc(id)+'" type="text" inputmode="numeric" autocomplete="off" placeholder="dd/mm/aaaa" value="'+esc(isoToDisplay(value))+'" aria-label="'+esc(label||'Selecionar data')+'" '+(readOnly?'readonly aria-readonly="true"':'')+'><input class="date-value" type="hidden" '+(name?'name="'+esc(name)+'"':'')+' value="'+esc(value||'')+'"><button class="date-trigger" type="button" aria-label="Abrir calendário" '+(readOnly?'disabled tabindex="-1"':'')+'><span aria-hidden="true"></span></button></div>'}
  function closeDatePickers(except){document.querySelectorAll('.date-popover.open').forEach(panel=>{if(panel!==except)panel.remove()});document.querySelectorAll('.app-date-field.open').forEach(field=>{if(field._panel!==except)field.classList.remove('open')});if(!except)state.datePicker=null}
  function setDateField(field,iso){field.querySelector('.date-value').value=iso||'';field.querySelector('.date-display').value=isoToDisplay(iso);field.dispatchEvent(new CustomEvent('app:datechange',{bubbles:true,detail:{value:iso||''}}))}
  function positionDatePicker(field,panel){if(window.innerWidth<=560){panel.removeAttribute('style');return}const rect=field.getBoundingClientRect(),width=312,left=Math.min(Math.max(10,rect.left),window.innerWidth-width-10),spaceBelow=window.innerHeight-rect.bottom,top=spaceBelow>380?rect.bottom+7:Math.max(10,rect.top-360);panel.style.left=left+'px';panel.style.top=top+'px';panel.style.width=width+'px'}
  function openDatePicker(field){const wasOpen=field.classList.contains('open');if(!wasOpen)document.dispatchEvent(new CustomEvent('superapp:control-open',{detail:{trigger:field}}));closeCustomSelects();if($('status-filter'))$('status-filter').open=false;closeDatePickers();if(wasOpen)return;const hidden=field.querySelector('.date-value'),selected=hidden.value||'',base=selected?new Date(selected+'T12:00:00'):new Date(),panel=document.createElement('div');let cursor=new Date(base.getFullYear(),base.getMonth(),1,12);panel.className='date-popover open';field._panel=panel;field.classList.add('open');document.body.appendChild(panel);panel.addEventListener('click',event=>event.stopPropagation());panel.addEventListener('pointerdown',event=>event.stopPropagation());state.datePicker=panel;
    function draw(){const year=cursor.getFullYear(),month=cursor.getMonth(),first=new Date(year,month,1,12),gridStart=new Date(first);gridStart.setDate(1-first.getDay());const today=new Date(),todayIso=[today.getFullYear(),String(today.getMonth()+1).padStart(2,'0'),String(today.getDate()).padStart(2,'0')].join('-');let days='';for(let i=0;i<42;i++){const day=new Date(gridStart);day.setDate(gridStart.getDate()+i);const iso=[day.getFullYear(),String(day.getMonth()+1).padStart(2,'0'),String(day.getDate()).padStart(2,'0')].join('-');days+='<button type="button" data-date="'+iso+'" class="'+(day.getMonth()!==month?'outside ':'')+(iso===selected?'selected ':'')+(iso===todayIso?'today':'')+'">'+day.getDate()+'</button>'}panel.innerHTML='<div class="date-popover-head"><button type="button" data-month="-1" aria-label="Mês anterior">‹</button><strong>'+esc(new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(cursor))+'</strong><button type="button" data-month="1" aria-label="Próximo mês">›</button></div><div class="date-weekdays"><span>D</span><span>S</span><span>T</span><span>Q</span><span>Q</span><span>S</span><span>S</span></div><div class="date-days">'+days+'</div><div class="date-popover-foot"><button type="button" data-date-today>Hoje</button><button type="button" data-date-clear>Limpar</button></div>';positionDatePicker(field,panel);panel.querySelectorAll('[data-month]').forEach(button=>button.onclick=()=>{cursor=new Date(year,month+Number(button.dataset.month),1,12);draw()});panel.querySelectorAll('[data-date]').forEach(button=>button.onclick=()=>{setDateField(field,button.dataset.date);closeDatePickers()});panel.querySelector('[data-date-today]').onclick=()=>{setDateField(field,todayIso);closeDatePickers()};panel.querySelector('[data-date-clear]').onclick=()=>{setDateField(field,'');closeDatePickers()}}
    draw()
  }
  function bindDatePickers(root){root.querySelectorAll('[data-date-field]:not([data-readonly="true"])').forEach(field=>{if(field.dataset.bound)return;field.dataset.bound='true';const display=field.querySelector('.date-display'),trigger=field.querySelector('.date-trigger');trigger.addEventListener('click',event=>{event.stopPropagation();openDatePicker(field)});display.addEventListener('focus',()=>openDatePicker(field));display.addEventListener('input',()=>{let digits=display.value.replace(/D/g,'').slice(0,8);if(digits.length>4)digits=digits.slice(0,2)+'/'+digits.slice(2,4)+'/'+digits.slice(4);else if(digits.length>2)digits=digits.slice(0,2)+'/'+digits.slice(2);display.value=digits});display.addEventListener('blur',()=>{const iso=displayToIso(display.value);if(iso)setDateField(field,iso);else if(!display.value)setDateField(field,'');else display.value=isoToDisplay(field.querySelector('.date-value').value)})})}
  async function loadLocations(){try{const response=await fetch('./ibge-locations.json');if(!response.ok)throw new Error('IBGE local indisponível');const data=await response.json();state.locations={states:Array.isArray(data.states)?data.states:[],cities:Array.isArray(data.cities)?data.cities:[]}}catch(error){console.warn(error);toast('Não foi possível carregar as localidades do IBGE.','error')}}
  async function loadWorks(){
    if(state.loadingWorks)return false;
    state.loadingWorks=true;state.loadError=false;setBusy(true);
    try{
      const [worksResult,franchisesResult,carpentersResult]=await Promise.allSettled([
        withTimeout(rpc('obras_list'),15000,'Tempo limite ao carregar obras'),
        withTimeout(rpc('obras_list_franchises'),15000,'Tempo limite ao carregar franquias'),
        withTimeout(rpc('obras_list_carpenters'),15000,'Tempo limite ao carregar carpinteiros')
      ]);
      if(worksResult.status==='rejected')throw worksResult.reason;
      state.works=rows(worksResult.value).filter(workVisibleToCurrentRole);state.timelineUndo.length=0;state.timelineRedo.length=0;
      if(franchisesResult.status==='fulfilled')state.franchises=rows(franchisesResult.value);
      if(carpentersResult.status==='fulfilled')state.carpenters=rows(carpentersResult.value);
      state.mapHasFit=false;renderFranchises();render();
      if(franchisesResult.status==='rejected'||carpentersResult.status==='rejected')toast('Obras carregadas. Alguns dados auxiliares estão temporariamente indisponíveis.','error');
      return true;
    }catch(error){
      state.loadError=true;state.works=[];renderFranchises();render();
      toast(safeMessage(error,'Não foi possível carregar as obras.'),'error');
      return false;
    }finally{state.loadingWorks=false;setBusy(false)}
  }
  function renderMapFailure(error){console.error('[obras:map-render]',error);const empty=$('map-empty'),locationList=$('location-list');if(empty){empty.textContent='Não foi possível exibir o mapa agora.';empty.hidden=false}if(locationList)locationList.innerHTML='<div class="map-load-error"><strong>Falha ao exibir as obras no mapa</strong><small>Os dados permanecem protegidos. Atualize a tela e tente novamente.</small><button class="btn" type="button" data-obras-render-retry>Tentar novamente</button></div>';locationList?.querySelector('[data-obras-render-retry]')?.addEventListener('click',()=>{state.mapHasFit=false;render()})}
  function render(){const list=filtered();$('result-count').textContent=list.length+' '+(list.length===1?'obra':'obras');if(state.view==='painel')renderKanban(list);if(state.view==='calendario')renderCalendar(list);if(state.view==='cronograma')renderTimeline(list);if(state.view==='locais'){try{renderMap(mapFiltered())}catch(error){renderMapFailure(error)}}}
function cardHtml(work){const priority=PRIORITY[work.priority]||PRIORITY.media,editable=canEditWork();return '<article class="work-card" draggable="'+String(editable)+'" data-id="'+esc(work.id)+'"><div class="card-top"><span class="priority-dot" style="--priority-color:'+priority+'" title="Prioridade '+esc(titleCase(work.priority))+'"></span><small>'+esc(work.code||work.franchise_name||'OBRA')+'</small></div><h3>'+esc(work.title)+'</h3><p>'+esc([work.city,work.state].filter(Boolean).join(' · ')||'Local ainda não definido')+'</p><div class="card-meta"><span>'+esc(dateLabel(work.start_date))+'</span><span>'+esc(work.responsible_name||'Sem responsável')+'</span></div><div class="card-footer"><div class="progress"><span style="width:'+Number(work.progress||0)+'%"></span></div><small>'+Number(work.progress||0)+'%</small></div>'+(editable?'<select class="mobile-status" data-status-id="'+esc(work.id)+'" aria-label="Mover obra">'+availableStatuses().map(item=>'<option value="'+item.id+'" '+(item.id===work.status?'selected':'')+'>'+esc(item.label)+'</option>').join('')+'</select>':'')+'</article>'}
  function renderKanban(list){$('kanban').innerHTML=availableStatuses().filter(status=>state.visibleStatuses.has(status.id)).map(status=>{const cards=sortWorks(list.filter(work=>work.status===status.id));return '<section class="kanban-column" style="--status-color:'+status.color+'"><header class="column-head"><strong>'+esc(status.label)+'</strong><span class="column-count">'+cards.length+'</span></header><div class="card-list" data-status="'+status.id+'">'+(cards.map(cardHtml).join('')||'<p class="empty-state">Nenhuma obra nesta etapa.</p>')+'</div></section>'}).join('');
    document.querySelectorAll('.work-card').forEach(card=>{card.addEventListener('click',e=>{if(e.target.matches('select'))return;openModal(state.works.find(w=>w.id===card.dataset.id))});card.addEventListener('dragstart',event=>{if(!canEditWork()){event.preventDefault();return}state.dragged=card.dataset.id;card.classList.add('dragging')});card.addEventListener('dragend',()=>{state.dragged=null;card.classList.remove('dragging')})});
    document.querySelectorAll('.card-list').forEach(listEl=>{listEl.addEventListener('dragover',e=>{e.preventDefault();listEl.classList.add('drag-over')});listEl.addEventListener('dragleave',()=>listEl.classList.remove('drag-over'));listEl.addEventListener('drop',e=>{e.preventDefault();listEl.classList.remove('drag-over');if(state.dragged&&canEditWork())moveWork(state.dragged,listEl.dataset.status)})});
    document.querySelectorAll('[data-status-id]').forEach(select=>select.addEventListener('change',e=>moveWork(select.dataset.statusId,e.target.value)));
  }
  async function moveWork(id,status){if(!canEditWork())return;const work=state.works.find(item=>item.id===id);if(!work||work.status===status)return;const before=work.status;work.status=status;render();try{const updated=await rpc('obras_update_status',{p_id:id,p_version:work.version,p_status:status,p_sort_order:Date.now()});Object.assign(work,updated);toast('Obra movida para '+statusBy(status).label+'.')}catch(error){work.status=before;render();toast(safeMessage(error,'Não foi possível mover a obra.'),'error');loadWorks()}}
  function shiftMonth(amount){state.calendar=new Date(state.calendar.getFullYear(),state.calendar.getMonth()+amount,1);renderCalendar()}
  function renderCalendar(list){list=list||filtered();const label=new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(state.calendar);$('calendar-label').textContent=label;
    const year=state.calendar.getFullYear(),month=state.calendar.getMonth(),start=new Date(year,month,1),gridStart=new Date(year,month,1-start.getDay()),weekdays=['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];let html=weekdays.map(d=>'<div class="weekday">'+d+'</div>').join('');const today=new Date();
    for(let i=0;i<42;i++){const day=new Date(gridStart);day.setDate(gridStart.getDate()+i);const iso=[day.getFullYear(),String(day.getMonth()+1).padStart(2,'0'),String(day.getDate()).padStart(2,'0')].join('-');const events=list.filter(w=>w.start_date&&iso>=w.start_date&&iso<=(w.end_date||w.start_date));html+='<div class="day '+(day.getMonth()!==month?'outside ':'')+(day.toDateString()===today.toDateString()?'today':'')+'"><span class="day-number">'+day.getDate()+'</span>'+events.slice(0,5).map(w=>'<button class="calendar-event" type="button" data-calendar-id="'+esc(w.id)+'" style="--event-color:'+statusBy(w.status).color+'">'+esc(w.title)+'</button>').join('')+(events.length>5?'<small>+'+(events.length-5)+' obras</small>':'')+'</div>'}
    $('calendar').innerHTML=html;bindWorkOpen('[data-calendar-id]','calendarId');
  }
  const TIMELINE_DAY=34,TIMELINE_DAYS=92;
  function isoDate(value){const date=value instanceof Date?value:new Date(value);return [date.getFullYear(),String(date.getMonth()+1).padStart(2,'0'),String(date.getDate()).padStart(2,'0')].join('-')}
  function parseIsoDate(value){return value?new Date(value+'T12:00:00'):null}
  function addIsoDays(value,amount){const date=parseIsoDate(value);if(!date)return null;date.setDate(date.getDate()+amount);return isoDate(date)}
  function isoDayDiff(start,end){const a=parseIsoDate(start),b=parseIsoDate(end);return a&&b?Math.round((b-a)/86400000):0}
  function derivedPeriod(items){const starts=[],ends=[];(Array.isArray(items)?items:[]).forEach(item=>{const start=item.start_date||item.end_date,end=item.end_date||item.start_date;if(start)starts.push(start);if(end)ends.push(end)});return{start:starts.length?starts.sort()[0]:null,end:ends.length?ends.sort().at(-1):null}}
  function workTimelinePeriod(work){return derivedPeriod((work.checklist_groups||[]).flatMap(group=>group.items||[]))}
  function timelineColor(value){let hash=0;for(const char of String(value||''))hash=(hash*31+char.charCodeAt(0))>>>0;return 'hsl('+(hash%360)+' 62% 47%)'}
  function shiftTimelineMonth(amount){state.timelineAnchor=new Date(state.timelineAnchor.getFullYear(),state.timelineAnchor.getMonth()+amount,1);state.timelineInitialized=true;renderTimeline(filtered(),true)}
  function timelineRange(list){if(!state.timelineInitialized){const dates=list.flatMap(work=>(work.checklist_groups||[]).flatMap(group=>(group.items||[]).flatMap(item=>[item.start_date,item.end_date].filter(Boolean)))).sort();if(dates.length)state.timelineAnchor=parseIsoDate(dates[0]);state.timelineInitialized=true}const start=new Date(state.timelineAnchor.getFullYear(),state.timelineAnchor.getMonth(),1,12);return{start:isoDate(start),end:addIsoDays(isoDate(start),TIMELINE_DAYS-1),days:TIMELINE_DAYS}}
  function timelineHeader(range){const days=[],months=[];let current=null;for(let index=0;index<range.days;index++){const iso=addIsoDays(range.start,index),date=parseIsoDate(iso),key=date.getFullYear()+'-'+date.getMonth(),weekend=date.getDay()===0||date.getDay()===6,today=iso===isoDate(new Date());days.push('<span class="gantt-day '+(weekend?'weekend ':'')+(today?'today':'')+'" title="'+esc(dateLabel(iso))+'"><b>'+date.getDate()+'</b><small>'+['D','S','T','Q','Q','S','S'][date.getDay()]+'</small></span>');if(!current||current.key!==key){current={key,start:index+1,count:0,label:new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(date)};months.push(current)}current.count++}return '<div class="gantt-months" style="grid-template-columns:repeat('+range.days+',var(--timeline-day))">'+months.map(month=>'<strong style="grid-column:'+month.start+' / span '+month.count+'">'+esc(month.label)+'</strong>').join('')+'</div><div class="gantt-days" style="grid-template-columns:repeat('+range.days+',var(--timeline-day))">'+days.join('')+'</div>'}
  function timelineBar(period,range,options={}){if(!period.start&&!period.end)return '';const start=period.start||period.end,end=period.end||period.start;if(end<range.start||start>range.end)return '';const visibleStart=start<range.start?range.start:start,visibleEnd=end>range.end?range.end:end,left=isoDayDiff(range.start,visibleStart)*TIMELINE_DAY+3,width=Math.max(12,(isoDayDiff(visibleStart,visibleEnd)+1)*TIMELINE_DAY-6),editable=options.kind==='item'&&canEditWork();return '<div class="gantt-bar '+esc(options.className||'')+(editable?' editable':' readonly')+'" style="left:'+left+'px;width:'+width+'px;--bar-color:'+esc(options.color||'var(--accent)')+'" data-work-id="'+esc(options.workId||'')+'" data-group-id="'+esc(options.groupId||'')+'" data-item-id="'+esc(options.itemId||'')+'" data-start="'+esc(start)+'" data-end="'+esc(end)+'" title="'+esc((options.label||'Período')+' · '+dateLabel(start)+' até '+dateLabel(end))+'">'+(editable?'<i class="gantt-resize start" data-resize="start" aria-hidden="true"></i>':'')+'<span>'+esc(options.label||'')+'</span>'+(editable?'<i class="gantt-resize end" data-resize="end" aria-hidden="true"></i>':'')+'</div>'}
  function timelineRow(label,className,bar){return '<div class="gantt-label '+className+'">'+label+'</div><div class="gantt-track '+className+'">'+bar+'</div>'}
  function renderTimeline(list,keepScroll=false){const root=$('timeline');if(!root)return;const previous=root.querySelector('.gantt-grid'),scrollLeft=keepScroll&&previous?previous.scrollLeft:0,range=timelineRange(list),startDate=parseIsoDate(range.start),endDate=parseIsoDate(range.end);$('timeline-label').textContent=new Intl.DateTimeFormat('pt-BR',{month:'short',year:'numeric'}).format(startDate)+' — '+new Intl.DateTimeFormat('pt-BR',{month:'short',year:'numeric'}).format(endDate);let rows='';sortWorks(list).forEach(work=>{const workCollapsed=state.timelineCollapsed.has(work.id),color=timelineColor(work.id),workPeriod=workTimelinePeriod(work);rows+=timelineRow('<button class="gantt-toggle" type="button" data-timeline-work="'+esc(work.id)+'" aria-expanded="'+String(!workCollapsed)+'"><span>▾</span><strong>'+esc(work.title)+'</strong></button>','level-work',timelineBar(workPeriod,range,{className:'work-period',color,label:work.title,workId:work.id}));if(workCollapsed)return;(work.checklist_groups||[]).forEach(group=>{const groupCollapsed=state.timelineGroupCollapsed.has(group.id),period=derivedPeriod(group.items||[]);rows+=timelineRow('<button class="gantt-toggle group" type="button" data-timeline-group="'+esc(group.id)+'" aria-expanded="'+String(!groupCollapsed)+'"><span>▾</span><strong>'+esc(group.title)+'</strong></button>','level-group',timelineBar(period,range,{className:'group-period',color,label:group.title,workId:work.id,groupId:group.id}));if(groupCollapsed)return;(group.items||[]).forEach(item=>{rows+=timelineRow('<div class="gantt-item-label '+(item.done?'done':'')+'"><span></span><strong title="'+esc(item.text)+'">'+esc(item.text)+'</strong></div>','level-item',timelineBar({start:item.start_date,end:item.end_date},range,{kind:'item',className:'item-period '+(item.done?'done':''),color,label:item.text,workId:work.id,groupId:group.id,itemId:item.id}))})})});root.innerHTML='<div class="gantt-grid" style="--timeline-days:'+range.days+';--timeline-width:'+(range.days*TIMELINE_DAY)+'px" data-range-start="'+range.start+'" data-range-days="'+range.days+'"><div class="gantt-corner">Obras e etapas</div><div class="gantt-header">'+timelineHeader(range)+'</div>'+rows+'</div>';const grid=root.querySelector('.gantt-grid');grid.scrollLeft=scrollLeft;root.querySelectorAll('[data-timeline-work]').forEach(button=>button.addEventListener('click',()=>{state.timelineCollapsed.has(button.dataset.timelineWork)?state.timelineCollapsed.delete(button.dataset.timelineWork):state.timelineCollapsed.add(button.dataset.timelineWork);renderTimeline(list,true)}));root.querySelectorAll('[data-timeline-group]').forEach(button=>button.addEventListener('click',()=>{state.timelineGroupCollapsed.has(button.dataset.timelineGroup)?state.timelineGroupCollapsed.delete(button.dataset.timelineGroup):state.timelineGroupCollapsed.add(button.dataset.timelineGroup);renderTimeline(list,true)}));bindTimelineBars(root,range)}
  function bindTimelineBars(root,range){
    root.querySelectorAll('.gantt-bar.editable').forEach(bar=>{
      bar.addEventListener('pointerdown',event=>beginTimelineDrag(event,bar,range));
      bar.addEventListener('click',event=>{event.stopPropagation();if(bar._timelineDragged){bar._timelineDragged=false;return}openTimelineChecklistItem(bar.dataset.workId,bar.dataset.groupId,bar.dataset.itemId)})
    })
  }
  function beginTimelineDrag(event,bar,range){
    if(event.button!==0||state.timelineSaving||state.timelineHistoryBusy)return;event.preventDefault();const mode=event.target.dataset.resize||'move',pointerId=event.pointerId,startX=event.clientX,originalStart=bar.dataset.start,originalEnd=bar.dataset.end||originalStart;let nextStart=originalStart,nextEnd=originalEnd;
    bar.setPointerCapture(pointerId);bar.classList.add('dragging');document.body.classList.add('timeline-dragging');
    const paint=()=>{const visibleStart=nextStart<range.start?range.start:nextStart,visibleEnd=nextEnd>range.end?range.end:nextEnd;bar.style.left=(isoDayDiff(range.start,visibleStart)*TIMELINE_DAY+3)+'px';bar.style.width=Math.max(12,(isoDayDiff(visibleStart,visibleEnd)+1)*TIMELINE_DAY-6)+'px';bar.title=dateLabel(nextStart)+' até '+dateLabel(nextEnd)};
    const move=moveEvent=>{const delta=Math.round((moveEvent.clientX-startX)/TIMELINE_DAY);if(mode==='start'){nextStart=addIsoDays(originalStart,delta);if(nextStart>originalEnd)nextStart=originalEnd}else if(mode==='end'){nextEnd=addIsoDays(originalEnd,delta);if(nextEnd<originalStart)nextEnd=originalStart}else{nextStart=addIsoDays(originalStart,delta);nextEnd=addIsoDays(originalEnd,delta)}paint()};
    const finish=async()=>{bar.removeEventListener('pointermove',move);bar.removeEventListener('pointerup',finish);bar.removeEventListener('pointercancel',cancel);document.body.classList.remove('timeline-dragging');bar.classList.remove('dragging');const changed=nextStart!==originalStart||nextEnd!==originalEnd;bar._timelineDragged=changed;if(changed)await saveTimelineItemDates(bar.dataset.workId,bar.dataset.groupId,bar.dataset.itemId,nextStart,nextEnd)};
    const cancel=()=>{document.body.classList.remove('timeline-dragging');bar._timelineDragged=true;renderTimeline(filtered(),true)};bar.addEventListener('pointermove',move);bar.addEventListener('pointerup',finish,{once:true});bar.addEventListener('pointercancel',cancel,{once:true})
  }
  async function saveTimelineItemDates(workId,groupId,itemId,startDate,endDate,options={}){
    if(!canEditWork())return false;const work=state.works.find(item=>item.id===workId),group=(work?.checklist_groups||[]).find(item=>item.id===groupId),item=group?.items?.find(row=>row.id===itemId);if(!work||!itemId||!item)return false;const beforeStart=options.beforeStart??item.start_date??null,beforeEnd=options.beforeEnd??item.end_date??null;state.timelineSaving=true;setBusy(true);
    try{const result=await rpc('obras_update_checklist_item_dates',{p_item_id:itemId,p_start_date:startDate,p_end_date:endDate,p_obra_version:work.version});work.version=Number(result.version);item.start_date=result.item_start_date;item.end_date=result.item_end_date;if(group){group.start_date=result.group_start_date;group.end_date=result.group_end_date}if(options.recordHistory!==false){state.timelineUndo.push({workId,groupId,itemId,beforeStart,beforeEnd,afterStart:result.item_start_date,afterEnd:result.item_end_date});if(state.timelineUndo.length>50)state.timelineUndo.shift();state.timelineRedo.length=0}renderTimeline(filtered(),true);if(!options.silent)toast('Datas da atividade atualizadas.');return true}catch(error){toast(safeMessage(error,'Não foi possível atualizar as datas.'),'error');await loadWorks();return false}finally{state.timelineSaving=false;setBusy(false)}
  }
  async function applyTimelineHistory(direction){
    if(state.view!=='cronograma'||state.timelineSaving||state.timelineHistoryBusy)return;const undo=direction==='undo',source=undo?state.timelineUndo:state.timelineRedo,target=undo?state.timelineRedo:state.timelineUndo,entry=source.pop();if(!entry)return;state.timelineHistoryBusy=true;try{const start=undo?entry.beforeStart:entry.afterStart,end=undo?entry.beforeEnd:entry.afterEnd,ok=await saveTimelineItemDates(entry.workId,entry.groupId,entry.itemId,start,end,{recordHistory:false,silent:true});if(ok){target.push(entry);toast(undo?'Alteração desfeita.':'Alteração refeita.')}}finally{state.timelineHistoryBusy=false}
  }
  function handleTimelineHistoryShortcut(event){
    if(state.view!=='cronograma'||!event.ctrlKey||event.altKey||event.metaKey||event.target.closest('input,textarea,select,[contenteditable="true"]'))return;const key=event.key.toLowerCase(),redo=key==='y'||(key==='z'&&event.shiftKey),undo=key==='z'&&!event.shiftKey;if(!undo&&!redo)return;event.preventDefault();applyTimelineHistory(redo?'redo':'undo')
  }
  function bindWorkOpen(selector,key){document.querySelectorAll(selector).forEach(el=>el.addEventListener('click',()=>openModal(state.works.find(w=>w.id===el.dataset[key]))))}
  function ensureMap(){if(!window.L&&window.leaflet)window.L=window.leaflet;if(state.map||!window.L)return;state.map=L.map('interactive-map',{zoomControl:true,minZoom:3,maxZoom:19}).setView([-14.2,-51.9],4);const streets=L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxNativeZoom:19,maxZoom:19,attribution:'&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'}),satellite=L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{maxNativeZoom:18,maxZoom:18,attribution:'Tiles &copy; Esri — Source: Esri and imagery providers'});streets.addTo(state.map);state.mapBaseLayers={Mapa:streets,'Satélite':satellite};L.control.layers(state.mapBaseLayers,null,{position:'topright',collapsed:window.innerWidth<=900}).addTo(state.map);state.map.on('baselayerchange',event=>{const max=Number(event.layer?.options?.maxZoom||19);state.map.setMaxZoom(max);if(state.map.getZoom()>max)state.map.setZoom(max)});state.mapLayer=L.layerGroup().addTo(state.map)}
  function markerIcon(){return L.divIcon({className:'work-map-div-icon',html:'<svg class="work-map-marker-svg" viewBox="0 0 32 42" aria-hidden="true"><path fill="#f9b215" stroke="#ffffff" stroke-width="2.5" d="M16 1.5C8.2 1.5 2 7.7 2 15.5 2 26 16 40.5 16 40.5S30 26 30 15.5C30 7.7 23.8 1.5 16 1.5Z"/><circle cx="16" cy="15.5" r="5.5" fill="#ffffff"/></svg>',iconSize:[32,42],iconAnchor:[16,42],popupAnchor:[0,-39]})}
  function renderMap(list){
    const entries=list.map(work=>({work,coords:workCoordinates(work)})),mapped=entries.filter(entry=>entry.coords),empty=$('map-empty'),locationList=$('location-list');
    if(state.loadError){empty.textContent='Não foi possível carregar as obras.';empty.hidden=false;locationList.innerHTML='<div class="map-load-error"><strong>Falha ao carregar as obras</strong><small>Verifique a conexão e tente novamente.</small><button class="btn" type="button" data-obras-retry>Tentar novamente</button></div>';locationList.querySelector('[data-obras-retry]').addEventListener('click',loadWorks);try{ensureMap();setTimeout(()=>state.map?.invalidateSize(),60)}catch(error){console.error('[obras:map]',error)}return}
    locationList.innerHTML=entries.length?entries.map(({work,coords})=>{const location=esc([work.city,work.state,work.franchise_name].filter(Boolean).join(' · ')||'Local ainda não definido');if(!coords)return '<article class="location-item location-item-unmapped"><div class="location-copy"><strong>'+esc(work.title)+'</strong><small>'+location+'</small><em>Localização não cadastrada</em></div></article>';const url=googleMapUrl({...work,latitude:coords.latitude,longitude:coords.longitude});return '<article class="location-item"><button type="button" data-location-id="'+esc(work.id)+'"><strong>'+esc(work.title)+'</strong><small>'+location+'</small></button><div class="location-actions"><a href="'+esc(url)+'" target="_blank" rel="noopener noreferrer">Abrir rota</a><button type="button" data-copy-map="'+esc(work.id)+'">Copiar link</button></div></article>'}).join(''):'<p class="empty-state">Nenhuma obra disponível no seu escopo de acesso.</p>';
    document.querySelectorAll('[data-copy-map]').forEach(button=>button.addEventListener('click',async()=>{const work=state.works.find(item=>item.id===button.dataset.copyMap);if(!work)return;try{await navigator.clipboard.writeText(googleMapUrl(work));toast('Link do Google Maps copiado.')}catch(_){toast('Não foi possível copiar o link neste navegador.','error')}}));
    if(!window.L){empty.textContent='O mapa não pôde ser iniciado. As obras continuam disponíveis na lista.';empty.hidden=false;return}
    try{ensureMap()}catch(error){console.error('[obras:map]',error);empty.textContent='O mapa não pôde ser iniciado. As obras continuam disponíveis na lista.';empty.hidden=false;return}
    if(!state.map){empty.textContent='O mapa não pôde ser iniciado. As obras continuam disponíveis na lista.';empty.hidden=false;return}
    state.mapLayer.clearLayers();state.mapMarkers.clear();
    if(!mapped.length){empty.textContent='Nenhuma obra com coordenadas no escopo atual.';empty.hidden=false;state.map.setView([-14.2,-51.9],4,{animate:false});setTimeout(()=>state.map.invalidateSize(),60);return}
    empty.hidden=true;const bounds=[];
    mapped.forEach(({work,coords})=>{const lat=coords.latitude,lon=coords.longitude,mapsUrl=googleMapUrl({...work,latitude:lat,longitude:lon}),marker=L.marker([lat,lon],{icon:markerIcon(),title:work.title}).bindPopup('<strong>'+esc(work.title)+'</strong><br><small>'+esc([work.city,work.state,work.franchise_name].filter(Boolean).join(' · '))+'</small><br><a class="map-popup-link" href="'+esc(mapsUrl)+'" target="_blank" rel="noopener noreferrer">Abrir no Google Maps ↗</a>',{autoPan:false});marker.addTo(state.mapLayer);state.mapMarkers.set(work.id,marker);bounds.push([lat,lon])});
    document.querySelectorAll('[data-location-id]').forEach(button=>button.addEventListener('click',()=>{
      const marker=state.mapMarkers.get(button.dataset.locationId);if(!marker)return;
      const centerMarker=()=>{state.map.invalidateSize({pan:false});state.map.closePopup();state.map.flyTo(marker.getLatLng(),15,{animate:true,duration:.72,easeLinearity:.2});marker.openPopup()};
      if(window.innerWidth<=900){$('map-stage').scrollIntoView({behavior:'smooth',block:'start'});setTimeout(centerMarker,280)}else centerMarker()
    }));
    if(bounds.length&&!state.mapHasFit){state.map.fitBounds(bounds,{padding:[45,45],maxZoom:13});state.mapHasFit=true}setTimeout(()=>state.map.invalidateSize(),60)
  }
  function fieldOptions(items,value){return items.map(item=>'<option value="'+esc(item.id)+'" '+(String(item.id)===String(value)?'selected':'')+'>'+esc(item.label||item.display_name)+'</option>').join('')}
  function stateOptions(value){return state.locations.states.map(item=>'<option value="'+esc(item.uf)+'" '+(item.uf===value?'selected':'')+'>'+esc(item.uf+' · '+item.name)+'</option>').join('')}
  function cityOptions(uf,value){const selected=state.locations.states.find(item=>item.uf===uf);if(!selected)return '';return state.locations.cities.filter(item=>String(item.state_id)===String(selected.id)).map(item=>'<option value="'+esc(item.name)+'" '+(item.name===value?'selected':'')+'>'+esc(item.name)+'</option>').join('')}
  function bindLocationFields(city){const uf=$('f-state'),citySelect=$('f-city');function populate(value){citySelect.innerHTML='<option value="">Selecione a cidade</option>'+cityOptions(uf.value,value);citySelect.disabled=!uf.value;refreshCustomSelect(citySelect)}uf.addEventListener('change',()=>populate(''));populate(city||'');$('parse-map-link').addEventListener('click',parseMapLink)}
  function extractCoordinates(raw){let text=String(raw||'').trim();for(let i=0;i<2;i++){try{text=decodeURIComponent(text)}catch(_){break}}const patterns=[/\/maps\/(?:search|place|dir)\/(?:[^/?#]+\/)?(-?\d{1,2}(?:\.\d+)?)[,+\s]+(-?\d{1,3}(?:\.\d+)?)(?:[/?#]|$)/i,/@(-?\d{1,2}(?:\.\d+)?),\s*(-?\d{1,3}(?:\.\d+)?)/,/!3d(-?\d{1,2}(?:\.\d+)?)!4d(-?\d{1,3}(?:\.\d+)?)/,/[?&](?:q|query|ll|daddr|destination)=(-?\d{1,2}(?:\.\d+)?)(?:%2C|,|\s)+(-?\d{1,3}(?:\.\d+)?)/i,/^\s*(-?\d{1,2}(?:\.\d+)?)\s*,\s*(-?\d{1,3}(?:\.\d+)?)\s*$/];for(const pattern of patterns){const match=text.match(pattern);if(match){const lat=Number(match[1]),lon=Number(match[2]);if(lat>=-90&&lat<=90&&lon>=-180&&lon<=180)return{lat,lon}}}return null}
  function applyCoordinates(coords){$('f-lat').value=Number(coords.lat??coords.latitude).toFixed(6);$('f-lon').value=Number(coords.lon??coords.longitude).toFixed(6)}
  async function parseMapLink(){const feedback=$('location-feedback'),button=$('parse-map-link'),raw=$('f-map-link').value.trim(),direct=extractCoordinates(raw);if(direct){applyCoordinates(direct);feedback.textContent='Coordenadas identificadas e preenchidas.';feedback.className='location-feedback success';return}let url;try{url=new URL(raw)}catch(_){feedback.textContent='Informe um link válido do Google Maps ou Apple Maps.';feedback.className='location-feedback error';return}const allowed=['maps.app.goo.gl','goo.gl','maps.google.com','maps.google.com.br','www.google.com','www.google.com.br','google.com','google.com.br','maps.apple.com'];if(url.protocol!=='https:'||!allowed.includes(url.hostname.toLowerCase())){feedback.textContent='Use somente um link HTTPS oficial do Google Maps ou Apple Maps.';feedback.className='location-feedback error';return}button.disabled=true;feedback.textContent='Resolvendo o link curto com segurança…';feedback.className='location-feedback';try{const result=await rpc('obras_resolve_map_link',{p_url:raw});if(!result||!Number.isFinite(Number(result.latitude))||!Number.isFinite(Number(result.longitude)))throw new Error('Coordenadas não encontradas');applyCoordinates(result);feedback.textContent='Link resolvido e coordenadas preenchidas.';feedback.className='location-feedback success'}catch(error){feedback.textContent=safeMessage(error,'Não foi possível identificar as coordenadas neste link.');feedback.className='location-feedback error'}finally{button.disabled=false}}
  function carpentersForFranchise(franchiseId,selectedId){return state.carpenters.filter(item=>item.franchise_id===franchiseId&&(item.active||item.id===selectedId)).sort((a,b)=>String(a.name).localeCompare(String(b.name),'pt-BR'))}
  function responsibleOptions(franchiseId,selectedId,legacyName){let html='<option value="">Sem responsável</option>';if(legacyName&&!selectedId)html+='<option value="legacy" data-name="'+esc(legacyName)+'" selected>'+esc(legacyName)+' · cadastro anterior</option>';html+=carpentersForFranchise(franchiseId,selectedId).map(item=>'<option value="'+esc(item.id)+'" data-name="'+esc(item.name)+'" '+(item.id===selectedId?'selected':'')+'>'+esc(item.name)+(item.active?'':' · inativo')+'</option>').join('');return html}
  function refreshResponsibleSelect(selectedId,legacyName){const select=$('f-responsible'),franchiseId=state.profile?.franchise_id||$('f-franchise').value;select.innerHTML=responsibleOptions(franchiseId,selectedId,legacyName);refreshCustomSelect(select);renderCarpenterList(franchiseId)}
  function renderCarpenterList(franchiseId){const list=$('carpenter-list');if(!list)return;const items=state.carpenters.filter(item=>item.franchise_id===franchiseId&&item.active).sort((a,b)=>String(a.name).localeCompare(String(b.name),'pt-BR'));list.innerHTML=items.length?items.map(item=>'<div class="carpenter-item"><span><strong>'+esc(item.name)+'</strong><small>'+esc(item.phone||'Sem telefone')+'</small></span><button class="icon-btn" type="button" data-carpenter-remove="'+esc(item.id)+'" aria-label="Desativar carpinteiro">×</button></div>').join(''):'<p class="empty-state">Nenhum carpinteiro cadastrado nesta franquia.</p>'}
  function bindCarpenterControls(work){const franchise=$('f-franchise'),manager=$('carpenter-manager'),toggle=$('manage-carpenters');function selectedFranchise(){return state.profile?.franchise_id||franchise.value}franchise.addEventListener('change',()=>refreshResponsibleSelect('',null));toggle.addEventListener('click',()=>{manager.classList.toggle('hidden');toggle.setAttribute('aria-expanded',String(!manager.classList.contains('hidden')))});$('carpenter-create').addEventListener('click',async()=>{const franchiseId=selectedFranchise(),name=$('new-carpenter-name').value.trim(),phone=$('new-carpenter-phone').value.trim();if(!franchiseId)return toast('Selecione uma franquia antes de cadastrar o carpinteiro.','error');if(name.length<2)return toast('Informe o nome do carpinteiro.','error');const button=$('carpenter-create');button.disabled=true;try{const created=await rpc('obras_create_carpenter',{p_franchise_id:franchiseId,p_name:name,p_phone:phone||null});state.carpenters.push(created);$('new-carpenter-name').value='';$('new-carpenter-phone').value='';refreshResponsibleSelect(created.id,null);toast('Carpinteiro cadastrado para a franquia.')}catch(error){toast(safeMessage(error,'Não foi possível cadastrar o carpinteiro.'),'error')}finally{button.disabled=false}});$('carpenter-list').addEventListener('click',async event=>{const button=event.target.closest('[data-carpenter-remove]');if(!button)return;if(!confirm('Desativar este carpinteiro? As obras antigas manterão o vínculo.'))return;button.disabled=true;try{await rpc('obras_deactivate_carpenter',{p_id:button.dataset.carpenterRemove});const item=state.carpenters.find(row=>row.id===button.dataset.carpenterRemove);if(item)item.active=false;if($('f-responsible').value===button.dataset.carpenterRemove)$('f-responsible').value='';refreshResponsibleSelect($('f-responsible').value,work?.responsible_name);toast('Carpinteiro desativado.')}catch(error){button.disabled=false;toast(safeMessage(error,'Não foi possível desativar o carpinteiro.'),'error')}});renderCarpenterList(selectedFranchise())}
  function cloneDefaultChecklistGroups(){return DEFAULT_CHECKLIST_GROUPS.map((group,gIndex)=>({id:'',title:group.title,position:gIndex,start_date:null,end_date:null,items:group.items.map((text,iIndex)=>({id:'',text,done:false,start_date:null,end_date:null,position:iIndex}))}))}
  function normalizeChecklistGroups(work,withDefaults=true){if(Array.isArray(work?.checklist_groups)&&work.checklist_groups.length)return work.checklist_groups.map((group,gIndex)=>({id:group.id||'',title:group.title||('Checklist '+(gIndex+1)),position:group.position??gIndex,start_date:group.start_date||null,end_date:group.end_date||null,items:Array.isArray(group.items)?group.items:[]}));if(Array.isArray(work?.checklist_items)&&work.checklist_items.length)return[{id:'',title:'Checklist da obra',position:0,start_date:null,end_date:null,items:work.checklist_items.map(item=>({...item,start_date:item.start_date||null,end_date:item.end_date||item.due_date||null}))}];return withDefaults?cloneDefaultChecklistGroups():[]}  function resizeChecklistText(textarea){if(!textarea||!textarea.isConnected)return;if(window.CSS?.supports?.('field-sizing','content')){textarea.style.removeProperty('height');textarea.style.overflowY='hidden';return}textarea.style.height='auto';textarea.style.height=Math.max(44,textarea.scrollHeight+2)+'px';textarea.style.overflowY='hidden'}
  function bindChecklistTextareas(root){root.querySelectorAll('.checklist-text').forEach(textarea=>{if(textarea.dataset.autosize!=='true'){textarea.dataset.autosize='true';textarea.addEventListener('input',()=>resizeChecklistText(textarea));textarea.addEventListener('focus',()=>resizeChecklistText(textarea))}resizeChecklistText(textarea)})}
  function checklistRowHtml(item){const key='check-'+Math.random().toString(36).slice(2);return '<div class="checklist-item-row" data-id="'+esc(item?.id||'')+'"><label class="checklist-check" title="Concluído"><input type="checkbox" '+(item?.done?'checked':'')+'><span></span></label><textarea class="checklist-text" maxlength="1000" rows="2" placeholder="Atividade do checklist">'+esc(item?.text||'')+'</textarea><div class="checklist-item-dates"><label><span>Início</span>'+dateFieldHtml(key+'-start','',item?.start_date||'','Data inicial da atividade')+'</label><label><span>Conclusão</span>'+dateFieldHtml(key+'-end','',item?.end_date||'','Data final da atividade')+'</label></div><button class="icon-btn checklist-remove" data-checklist-remove type="button" aria-label="Remover atividade">×</button></div>'}
  function checklistGroupHtml(group,index){const items=Array.isArray(group.items)?group.items:[],done=items.filter(item=>item.done).length,pct=items.length?Math.round(done/items.length*100):0,key='group-'+Math.random().toString(36).slice(2);return '<section class="work-checklist-group" data-group-id="'+esc(group.id||'')+'"><header class="work-checklist-head"><button class="checklist-collapse" type="button" data-checklist-toggle aria-label="Recolher ou expandir" aria-expanded="true"><span>▾</span></button><label class="checklist-group-title"><span class="sr-only">Nome do checklist</span><input maxlength="140" value="'+esc(group.title||('Checklist '+(index+1)))+'"></label><strong class="checklist-group-pct">'+pct+'%</strong><button class="btn checklist-delete-group" type="button" data-checklist-delete-group>Excluir</button></header><div class="checklist-progress"><span style="width:'+pct+'%"></span></div><div class="checklist-collapsible"><div class="checklist-collapsible-inner"><div class="checklist-group-period"><label><span>Início automático da etapa</span>'+dateFieldHtml(key+'-start','',group.start_date||'','Início automático da etapa',true)+'</label><label><span>Conclusão automática da etapa</span>'+dateFieldHtml(key+'-end','',group.end_date||'','Conclusão automática da etapa',true)+'</label></div><div class="work-checklist-body"><div class="work-checklist-items">'+items.map(checklistRowHtml).join('')+'</div><button class="btn checklist-add-item" type="button" data-checklist-add-item>+ Adicionar atividade</button></div></div></div></section>'}
  function syncChecklistGroupDates(group){const rows=[...group.querySelectorAll('.checklist-item-row')],items=rows.map(row=>{const values=row.querySelectorAll('.checklist-item-dates .date-value');return{start_date:values[0]?.value||null,end_date:values[1]?.value||null}}),period=derivedPeriod(items),fields=group.querySelectorAll('.checklist-group-period .app-date-field');[[fields[0],period.start],[fields[1],period.end]].forEach(([field,value])=>{if(!field)return;field.querySelector('.date-value').value=value||'';field.querySelector('.date-display').value=isoToDisplay(value||'')});return period}
  function setChecklistCollapsed(group,collapsed){const button=group.querySelector('[data-checklist-toggle]'),content=group.querySelector('.checklist-collapsible-inner');if(collapsed)closeDatePickers();group.classList.toggle('recolhido',collapsed);button?.setAttribute('aria-expanded',String(!collapsed));if(content)content.inert=collapsed}
  function updateChecklistSummary(){
    const groups=[...document.querySelectorAll('#checklist-list .work-checklist-group')];let total=0,done=0;groups.forEach(group=>{const rows=[...group.querySelectorAll('.checklist-item-row')];rows.forEach(row=>row.classList.toggle('done',Boolean(row.querySelector('.checklist-check input')?.checked)));const finished=rows.filter(row=>row.classList.contains('done')).length;total+=rows.length;done+=finished;const pct=rows.length?Math.round(finished/rows.length*100):0;group.querySelector('.checklist-group-pct').textContent=pct+'%';group.querySelector('.checklist-progress span').style.width=pct+'%';syncChecklistGroupDates(group)});$('checklist-summary').textContent=groups.length+' checklist(s) · '+done+' de '+total+' atividades concluídas'
  }
  function renderChecklistEditor(groups){const list=$('checklist-list'),normalized=Array.isArray(groups)?groups:[];list.innerHTML=normalized.map(checklistGroupHtml).join('');bindDatePickers(list);bindChecklistTextareas(list);list.addEventListener('click',event=>{const group=event.target.closest('.work-checklist-group');if(!group)return;const toggle=event.target.closest('[data-checklist-toggle]');if(toggle){event.preventDefault();event.stopPropagation();setChecklistCollapsed(group,group.dataset.collapsed!=='true');return}if(event.target.closest('[data-checklist-delete-group]')){if(confirm('Excluir este checklist e todas as atividades?'))group.remove()}if(event.target.closest('[data-checklist-add-item]')){group.querySelector('.work-checklist-items').insertAdjacentHTML('beforeend',checklistRowHtml({}));bindDatePickers(group);bindChecklistTextareas(group);group.querySelector('.work-checklist-items').lastElementChild.querySelector('.checklist-text').focus()}const remove=event.target.closest('[data-checklist-remove]');if(remove)remove.closest('.checklist-item-row').remove();updateChecklistSummary()});list.addEventListener('change',updateChecklistSummary);list.addEventListener('app:datechange',updateChecklistSummary);$('checklist-add-group').addEventListener('click',()=>{list.insertAdjacentHTML('beforeend',checklistGroupHtml({id:'',title:'Novo checklist',start_date:null,end_date:null,items:[]},list.children.length));bindDatePickers(list.lastElementChild);bindChecklistTextareas(list.lastElementChild);updateChecklistSummary();list.lastElementChild.querySelector('.checklist-group-title input').focus()});updateChecklistSummary()}
  function collectChecklist(){return [...document.querySelectorAll('#checklist-list .work-checklist-group')].map((group,gIndex)=>{const items=[...group.querySelectorAll('.checklist-item-row')].map((row,iIndex)=>({id:row.dataset.id||null,text:row.querySelector('.checklist-text').value.trim(),done:row.querySelector('.checklist-check input').checked,start_date:row.querySelectorAll('.checklist-item-dates .date-value')[0]?.value||null,end_date:row.querySelectorAll('.checklist-item-dates .date-value')[1]?.value||null,position:iIndex})).filter(item=>item.text),period=derivedPeriod(items);return{id:group.dataset.groupId||null,title:group.querySelector('.checklist-group-title input').value.trim()||('Checklist '+(gIndex+1)),position:gIndex,start_date:period.start,end_date:period.end,items}}).filter(group=>group.title)}
  function bindModalTabs(){const buttons=[...document.querySelectorAll('[data-modal-tab]')],panes=[...document.querySelectorAll('[data-modal-pane]')];buttons.forEach(button=>button.addEventListener('click',()=>{buttons.forEach(item=>{const active=item===button;item.classList.toggle('active',active);item.setAttribute('aria-selected',String(active))});closeCustomSelects();panes.forEach(pane=>pane.classList.toggle('hidden',pane.dataset.modalPane!==button.dataset.modalTab));$('work-fields').scrollTop=0;requestAnimationFrame(()=>bindChecklistTextareas($('work-fields')))}))}
  function focusChecklistTarget(target){
    const checklistTab=[...document.querySelectorAll('[data-modal-tab]')].find(button=>button.dataset.modalTab==='checklist');checklistTab?.click();
    const group=[...document.querySelectorAll('#checklist-list .work-checklist-group')].find(row=>String(row.dataset.groupId)===String(target.groupId));
    if(group?.dataset.collapsed==='true')setChecklistCollapsed(group,false);
    const item=[...(group||$('checklist-list')).querySelectorAll('.checklist-item-row')].find(row=>String(row.dataset.id)===String(target.itemId));
    requestAnimationFrame(()=>requestAnimationFrame(()=>{if(!item)return;const scroller=$('work-fields'),itemRect=item.getBoundingClientRect(),scrollerRect=scroller.getBoundingClientRect(),targetTop=scroller.scrollTop+(itemRect.top-scrollerRect.top)-(scroller.clientHeight-itemRect.height)/2;scroller.scrollTo({top:Math.max(0,targetTop),behavior:'smooth'});item.classList.add('checklist-target-focus');setTimeout(()=>item.classList.remove('checklist-target-focus'),1500);const field=item.querySelector('.checklist-item-title');field?.focus({preventScroll:true});field?.select()}))
  }
  let driveLinksDraft=[];
  function normalizeDriveUrl(value){
    try{
      const url=new URL(String(value||'').trim());
      if(url.protocol!=='https:'||!['drive.google.com','docs.google.com'].includes(url.hostname.toLowerCase()))return null;
      url.hash='';
      return url.href.slice(0,1000);
    }catch{return null}
  }
  function driveFileId(value){
    const url=normalizeDriveUrl(value);if(!url)return '';
    const pathMatch=url.match(/\/d\/([A-Za-z0-9_-]{10,})/),queryMatch=new URL(url).searchParams.get('id');
    return pathMatch?.[1]||(/^[A-Za-z0-9_-]{10,}$/.test(queryMatch||'')?queryMatch:'');
  }
  function renderDriveLinks(readOnly){
    const list=$('drive-links-list');if(!list)return;list.replaceChildren();
    if(!driveLinksDraft.length){const empty=document.createElement('p');empty.className='drive-link-empty';empty.textContent='Nenhum arquivo do Google Drive vinculado.';list.appendChild(empty);return}
    driveLinksDraft.forEach((item,index)=>{
      const card=document.createElement('article');card.className='drive-link-card';
      const link=document.createElement('a');link.href=item.url;link.target='_blank';link.rel='noopener noreferrer';link.className='drive-link-preview';link.setAttribute('aria-label','Abrir '+(item.title||'arquivo do Google Drive'));
      const fileId=driveFileId(item.url);
      if(fileId){const img=document.createElement('img');img.src='https://drive.google.com/thumbnail?id='+encodeURIComponent(fileId)+'&sz=w480';img.alt='';img.loading='lazy';img.referrerPolicy='no-referrer';img.addEventListener('error',()=>{img.replaceWith(Object.assign(document.createElement('span'),{className:'drive-link-icon',textContent:item.type==='video'?'\u25b6':item.type==='pdf'?'PDF':'ARQ'}))});link.appendChild(img)}
      else{const icon=document.createElement('span');icon.className='drive-link-icon';icon.textContent=item.type==='video'?'\u25b6':item.type==='pdf'?'PDF':item.type==='image'?'IMG':'ARQ';link.appendChild(icon)}
      const meta=document.createElement('span');meta.className='drive-link-meta';const title=document.createElement('strong');title.textContent=item.title||'Arquivo do Google Drive';const type=document.createElement('small');type.textContent=({image:'Foto',video:'V\u00eddeo',pdf:'PDF',file:'Arquivo'})[item.type]||'Arquivo';meta.append(title,type);link.appendChild(meta);card.appendChild(link);
      if(!readOnly){const remove=document.createElement('button');remove.type='button';remove.className='icon-btn drive-link-remove';remove.setAttribute('aria-label','Remover v\u00ednculo');remove.textContent='\u00d7';remove.addEventListener('click',()=>{driveLinksDraft.splice(index,1);renderDriveLinks(false)});card.appendChild(remove)}
      list.appendChild(card);
    });
  }
  function mountDriveLinks(work,readOnly){
    driveLinksDraft=Array.isArray(work?.drive_links)?work.drive_links.map(item=>({url:normalizeDriveUrl(item?.url),title:String(item?.title||'').slice(0,140),type:['image','video','pdf','file'].includes(item?.type)?item.type:'file'})).filter(item=>item.url):[];
    const grid=$('work-fields')?.querySelector('[data-modal-pane="geral"] .form-grid'),description=$('f-description')?.closest('.field');if(!grid)return;
    const field=document.createElement('div');field.className='field full drive-links-field';field.innerHTML='<label>Arquivos do Google Drive</label><div class="drive-link-create"><input id="f-drive-url" type="url" inputmode="url" maxlength="1000" placeholder="Cole um link p\u00fablico do Google Drive"><input id="f-drive-title" maxlength="140" placeholder="Nome do arquivo"><select id="f-drive-type"><option value="image">Foto</option><option value="video">V\u00eddeo</option><option value="pdf">PDF</option><option value="file">Outro arquivo</option></select><button class="btn" id="drive-link-add" type="button">Adicionar</button></div><small class="field-hint">Somente links HTTPS do Google Drive. O arquivo permanece no Drive e deve possuir compartilhamento apropriado.</small><div class="drive-links-list" id="drive-links-list"></div>';
    grid.insertBefore(field,description||null);
    if(readOnly){field.querySelector('.drive-link-create').hidden=true}
    else $('drive-link-add').addEventListener('click',()=>{
      const url=normalizeDriveUrl($('f-drive-url').value),title=$('f-drive-title').value.trim(),type=$('f-drive-type').value;
      if(!url)return toast('Use um link HTTPS v\u00e1lido do Google Drive.','error');
      if(driveLinksDraft.length>=12)return toast('Limite de 12 arquivos por obra.','error');
      if(driveLinksDraft.some(item=>item.url===url))return toast('Este arquivo j\u00e1 foi vinculado.','error');
      driveLinksDraft.push({url,title:title||'Arquivo do Google Drive',type});$('f-drive-url').value='';$('f-drive-title').value='';renderDriveLinks(false);
    });
    renderDriveLinks(readOnly);
  }
  function collectDriveLinks(){return driveLinksDraft.map(item=>({url:item.url,title:item.title,type:item.type}))}
  function openModal(work,focusTarget){state.editing=work||null;const isEdit=Boolean(work);$('modal-title').textContent=isEdit?'Editar obra':'Nova obra';$('modal-eyebrow').textContent=isEdit?'ACOMPANHAMENTO':'CADASTRO';const readOnly=isEdit?!canEditWork():!canCreateWork();$('delete-btn').classList.toggle('hidden',!isEdit||!canDeleteWork());$('save-btn').classList.toggle('hidden',readOnly);$('cancel-btn').textContent=readOnly?'Fechar':'Cancelar';if(readOnly)$('modal-title').textContent='Detalhes da obra';const own=state.profile?.franchise_id||'',franchiseValue=work?.franchise_id||own||(state.franchises.length===1?state.franchises[0].id:''),groups=normalizeChecklistGroups(work,true),statusOptions=availableStatuses();
    $('work-fields').innerHTML='<div class="modal-tabs" role="tablist"><button class="active" type="button" role="tab" aria-selected="true" data-modal-tab="geral">Informações gerais</button><button type="button" role="tab" aria-selected="false" data-modal-tab="checklist">Checklists <span>'+groups.length+'</span></button></div><section class="modal-pane" data-modal-pane="geral"><div class="form-grid"><div class="field full"><label for="f-title">Nome da obra *</label><input id="f-title" name="title" maxlength="180" required value="'+esc(work?.title||'')+'"></div><div class="field"><label for="f-franchise">Franquia *</label><select id="f-franchise" name="franchise_id" required '+(state.profile?.franchise_id?'disabled':'')+'><option value="">Selecione</option>'+fieldOptions(state.franchises.map(f=>({id:f.id,label:f.display_name})),franchiseValue)+'</select></div><div class="field"><label for="f-status">Etapa</label><select id="f-status" name="status">'+fieldOptions(statusOptions,work?.status||'planejamento')+'</select></div><div class="field"><label for="f-priority">Prioridade</label><select id="f-priority" name="priority">'+fieldOptions([{id:'baixa',label:'Baixa'},{id:'media',label:'Média'},{id:'alta',label:'Alta'},{id:'urgente',label:'Urgente'}],work?.priority||'media')+'</select></div><div class="field"><label for="f-progress">Progresso (%)</label><input id="f-progress" name="progress" type="number" min="0" max="100" value="'+Number(work?.progress||0)+'"></div><div class="field"><label for="f-start">Início previsto</label>'+dateFieldHtml('f-start','start_date',work?.start_date||'','Início previsto')+'</div><div class="field"><label for="f-end">Conclusão prevista</label>'+dateFieldHtml('f-end','end_date',work?.end_date||'','Conclusão prevista')+'</div><div class="field full responsible-field"><div class="field-label-row"><label for="f-responsible">Responsável pela obra</label><button class="text-btn" id="manage-carpenters" type="button" aria-expanded="false">Gerenciar carpinteiros</button></div><select id="f-responsible" name="responsible_carpenter_id">'+responsibleOptions(franchiseValue,work?.responsible_carpenter_id,work?.responsible_name)+'</select><section class="carpenter-manager hidden" id="carpenter-manager"><div class="carpenter-create-row"><input id="new-carpenter-name" maxlength="120" placeholder="Nome do carpinteiro"><input id="new-carpenter-phone" maxlength="30" inputmode="tel" placeholder="Telefone opcional"><button class="btn" id="carpenter-create" type="button">Cadastrar</button></div><div class="carpenter-list" id="carpenter-list"></div></section></div><div class="field"><label for="f-state">Estado (UF)</label><select id="f-state" name="state"><option value="">Selecione a UF</option>'+stateOptions(work?.state||'')+'</select></div><div class="field"><label for="f-city">Cidade</label><select id="f-city" name="city" disabled><option value="">Selecione a cidade</option></select></div><div class="field full"><label for="f-address">Endereço da obra</label><input id="f-address" name="address" maxlength="240" value="'+esc(work?.address||'')+'"></div><div class="field full"><label for="f-map-link">Link do Google Maps ou Apple Maps</label><div class="field-row-action"><div class="field"><input id="f-map-link" type="url" inputmode="url" placeholder="Cole o link completo do mapa"></div><button class="btn" id="parse-map-link" type="button">Usar localização</button></div><small class="location-feedback" id="location-feedback">Também é possível informar as coordenadas manualmente abaixo.</small></div><div class="field"><label for="f-lat">Latitude</label><input id="f-lat" name="latitude" type="number" min="-90" max="90" step="any" value="'+esc(work?.latitude??'')+'"><small class="field-hint">Ex.: -27.596900</small></div><div class="field"><label for="f-lon">Longitude</label><input id="f-lon" name="longitude" type="number" min="-180" max="180" step="any" value="'+esc(work?.longitude??'')+'"><small class="field-hint">Ex.: -48.549500</small></div><div class="field full"><label for="f-description">Observações</label><textarea id="f-description" name="description" maxlength="3000">'+esc(work?.description||'')+'</textarea></div></div></section><section class="modal-pane hidden" data-modal-pane="checklist"><section class="checklist-editor"><div class="checklist-head"><div><strong>Checklists da obra</strong><small id="checklist-summary"></small></div><button class="btn" id="checklist-add-group" type="button">+ Novo checklist</button></div><p class="checklist-help">As datas das etapas e atividades são opcionais e ajudam na organização da obra.</p><div class="checklist-list" id="checklist-list"></div></section></section>';
    mountDriveLinks(work,readOnly);
    if(state.profile?.franchise_id)$('f-franchise').value=state.profile.franchise_id;bindLocationFields(work?.city||'');bindCarpenterControls(work);renderChecklistEditor(groups);bindModalTabs();enhanceSelects($('work-fields'));bindDatePickers($('work-fields'));if(readOnly){document.querySelectorAll('#work-fields input,#work-fields select,#work-fields textarea').forEach(control=>control.disabled=true);document.querySelectorAll('#work-fields .date-trigger,#work-fields .text-btn,#work-fields [data-checklist-add-item],#work-fields [data-checklist-remove],#work-fields [data-checklist-delete-group],#work-fields #checklist-add-group').forEach(control=>control.hidden=true)}document.documentElement.classList.add('modal-open');document.body.classList.add('modal-open');$('modal-backdrop').classList.add('open');$('modal-backdrop').setAttribute('aria-hidden','false');setTimeout(()=>{bindChecklistTextareas($('work-fields'));if(focusTarget?.itemId)focusChecklistTarget(focusTarget);else $('f-title').focus()},30)
  }
  function closeModal(){closeFloatingMenus();document.querySelectorAll('.select-popover').forEach(panel=>{if(panel._select&&$('work-fields').contains(panel._select))panel.remove()});document.documentElement.classList.remove('modal-open');document.body.classList.remove('modal-open');$('modal-backdrop').classList.remove('open');$('modal-backdrop').setAttribute('aria-hidden','true');state.editing=null}
  function formRecord(){const form=new FormData($('work-form')),num=name=>{const value=form.get(name);return value===''?null:Number(value)},responsible=$('f-responsible'),responsibleValue=String(responsible?.value||''),responsibleOption=responsible?.options[responsible.selectedIndex];return{franchise_id:state.profile?.franchise_id||String(form.get('franchise_id')||''),title:String(form.get('title')||'').trim(),status:String(form.get('status')||'planejamento'),priority:String(form.get('priority')||'media'),progress:num('progress')??0,start_date:String(form.get('start_date')||'')||null,end_date:String(form.get('end_date')||'')||null,responsible_carpenter_id:responsibleValue&&responsibleValue!=='legacy'?responsibleValue:null,responsible_name:responsibleOption?.dataset?.name||null,city:String(form.get('city')||'').trim()||null,state:String(form.get('state')||'').trim().toUpperCase()||null,address:String(form.get('address')||'').trim()||null,latitude:num('latitude'),longitude:num('longitude'),description:String(form.get('description')||'').trim()||null}}  async function saveWork(event){event.preventDefault();if((state.editing&&!canEditWork())||(!state.editing&&!canCreateWork()))return toast('Você possui acesso somente para visualização.','error');const record=formRecord(),checklist=collectChecklist(),invalidGroupDate=checklist.find(group=>group.start_date&&group.end_date&&group.end_date<group.start_date),invalidDate=checklist.flatMap(group=>group.items).find(item=>item.start_date&&item.end_date&&item.end_date<item.start_date);if(invalidGroupDate)return toast('A conclusão de uma etapa não pode ser anterior ao início.','error');if(invalidDate)return toast('A conclusão de uma atividade não pode ser anterior ao início.','error');if(!record.franchise_id)return toast('Selecione a franquia da obra.','error');if(record.end_date&&record.start_date&&record.end_date<record.start_date)return toast('A conclusão não pode ser anterior ao início.','error');setBusy(true);try{let saved;if(state.editing)saved=await rpc('obras_update',{p_id:state.editing.id,p_version:state.editing.version,p_record:record});else saved=await rpc('obras_create',{p_record:record});saved=await rpc('obras_replace_checklist',{p_obra_id:saved.id,p_version:saved.version,p_items:checklist});const wasEditing=Boolean(state.editing),index=state.works.findIndex(w=>w.id===saved.id);if(index>=0)state.works[index]=saved;else state.works.push(saved);state.mapHasFit=false;closeModal();await loadWorks();toast(wasEditing?'Obra e checklist atualizados.':'Obra criada para a franquia.')}catch(error){toast(safeMessage(error,'Não foi possível salvar a obra.'),'error')}finally{setBusy(false)}}
  async function saveWork(event){
    event.preventDefault();
    if((state.editing&&!canEditWork())||(!state.editing&&!canCreateWork()))return toast('Voc\u00ea possui acesso somente para visualiza\u00e7\u00e3o.','error');
    const record=formRecord(),checklist=collectChecklist(),driveLinks=collectDriveLinks();
    const invalidGroupDate=checklist.find(group=>group.start_date&&group.end_date&&group.end_date<group.start_date);
    const invalidDate=checklist.flatMap(group=>group.items).find(item=>item.start_date&&item.end_date&&item.end_date<item.start_date);
    if(invalidGroupDate)return toast('A conclus\u00e3o de uma etapa n\u00e3o pode ser anterior ao in\u00edcio.','error');
    if(invalidDate)return toast('A conclus\u00e3o de uma atividade n\u00e3o pode ser anterior ao in\u00edcio.','error');
    if(!record.franchise_id)return toast('Selecione a franquia da obra.','error');
    if(record.end_date&&record.start_date&&record.end_date<record.start_date)return toast('A conclus\u00e3o n\u00e3o pode ser anterior ao in\u00edcio.','error');
    setBusy(true);
    try{
      let saved;
      if(state.editing)saved=await rpc('obras_update',{p_id:state.editing.id,p_version:state.editing.version,p_record:record});
      else saved=await rpc('obras_create',{p_record:record});
      saved=await rpc('obras_replace_checklist',{p_obra_id:saved.id,p_version:saved.version,p_items:checklist});
      if(driveLinks.length||(state.editing?.drive_links||[]).length)saved=await rpc('obras_replace_drive_links',{p_obra_id:saved.id,p_version:saved.version,p_links:driveLinks});
      const wasEditing=Boolean(state.editing),index=state.works.findIndex(work=>work.id===saved.id);
      if(index>=0)state.works[index]=saved;else state.works.push(saved);
      state.mapHasFit=false;closeModal();await loadWorks();
      toast(wasEditing?'Obra, checklist e arquivos atualizados.':'Obra criada para a franquia.');
    }catch(error){toast(safeMessage(error,'N\u00e3o foi poss\u00edvel salvar a obra.'),'error')}
    finally{setBusy(false)}
  }
  async function deleteWork(){if(!canDeleteWork())return toast('Você não possui permissão para excluir esta obra.','error');const work=state.editing;if(!work||!confirm('Excluir esta obra? Esta ação não pode ser desfeita.'))return;setBusy(true);try{await rpc('obras_delete',{p_id:work.id,p_version:work.version});state.works=state.works.filter(w=>w.id!==work.id);closeModal();render();toast('Obra excluída.')}catch(error){toast(safeMessage(error,'Não foi possível excluir a obra.'),'error')}finally{setBusy(false)}}

  /* Obras V22: confirmação interna, checklist com nome/descrição e cronograma refinado */
  function checklistParts(item){
    const source=String(item?.description??item?.text??'').trim();
    let title=String(item?.title||'').trim(),description=source;
    if(!title){
      const match=source.match(/^(.{1,140}?)\s+-\s+([\s\S]+)$/);
      if(match){title=match[1].trim();description=match[2].trim()}
      else{title=source.slice(0,140)||'Nova tarefa';description=''}
    }
    return{...item,title:title.slice(0,140),description:description.slice(0,1000)}
  }
  function cloneDefaultChecklistGroups(){return DEFAULT_CHECKLIST_GROUPS.map((group,gIndex)=>({id:'',title:group.title,position:gIndex,start_date:null,end_date:null,items:group.items.map((text,iIndex)=>({...checklistParts({text}),id:'',done:false,start_date:null,end_date:null,position:iIndex}))}))}
  function normalizeChecklistGroups(work,withDefaults=true){
    if(Array.isArray(work?.checklist_groups)&&work.checklist_groups.length)return work.checklist_groups.map((group,gIndex)=>({id:group.id||'',title:group.title||('Checklist '+(gIndex+1)),position:group.position??gIndex,start_date:group.start_date||null,end_date:group.end_date||null,items:Array.isArray(group.items)?group.items.map(checklistParts):[]}));
    if(Array.isArray(work?.checklist_items)&&work.checklist_items.length)return[{id:'',title:'Checklist da obra',position:0,start_date:null,end_date:null,items:work.checklist_items.map(item=>checklistParts({...item,start_date:item.start_date||null,end_date:item.end_date||item.due_date||null}))}];
    return withDefaults?cloneDefaultChecklistGroups():[]
  }
  function checklistRowHtml(item){
    const key='check-'+Math.random().toString(36).slice(2),value=checklistParts(item);
    return '<div class="checklist-item-row" data-id="'+esc(value.id||'')+'"><label class="checklist-check" title="Concluído"><input type="checkbox" '+(value.done?'checked':'')+'><span></span></label><div class="checklist-item-copy"><label><span>Nome da tarefa</span><input class="checklist-item-title" maxlength="140" value="'+esc(value.title)+'" placeholder="Nome da tarefa"></label><label><span>Descrição</span><textarea class="checklist-text" maxlength="1000" rows="2" placeholder="Descrição da tarefa">'+esc(value.description)+'</textarea></label></div><div class="checklist-item-dates"><label><span>Início</span>'+dateFieldHtml(key+'-start','',value.start_date||'','Data inicial da atividade')+'</label><label><span>Conclusão</span>'+dateFieldHtml(key+'-end','',value.end_date||'','Data final da atividade')+'</label></div><button class="icon-btn checklist-remove" data-checklist-remove type="button" aria-label="Remover atividade">×</button></div>'
  }
  function collectChecklist(){return [...document.querySelectorAll('#checklist-list .work-checklist-group')].map((group,gIndex)=>{const items=[...group.querySelectorAll('.checklist-item-row')].map((row,iIndex)=>{const title=row.querySelector('.checklist-item-title')?.value.trim()||'',description=row.querySelector('.checklist-text')?.value.trim()||'',finalTitle=title||description.slice(0,140)||('Tarefa '+(iIndex+1));return{id:row.dataset.id||null,title:finalTitle,description,text:description,done:row.querySelector('.checklist-check input').checked,start_date:row.querySelectorAll('.checklist-item-dates .date-value')[0]?.value||null,end_date:row.querySelectorAll('.checklist-item-dates .date-value')[1]?.value||null,position:iIndex}}).filter(item=>item.title),period=derivedPeriod(items);return{id:group.dataset.groupId||null,title:group.querySelector('.checklist-group-title input').value.trim()||('Checklist '+(gIndex+1)),position:gIndex,start_date:period.start,end_date:period.end,items}}).filter(group=>group.title)}
  function setChecklistCollapsed(group,collapsed){
    const button=group.querySelector('[data-checklist-toggle]'),wrapper=group.querySelector('.checklist-collapsible'),content=group.querySelector('.checklist-collapsible-inner');
    group._checklistAnimation?.cancel();group.dataset.collapsed=String(collapsed);button?.setAttribute('aria-expanded',String(!collapsed));
    if(collapsed){
      closeDatePickers();if(content)content.inert=true;
      const height=Math.max(wrapper?.scrollHeight||0,1);
      if(wrapper?.animate){wrapper.style.transition='none';group._checklistAnimation=wrapper.animate([{height:height+'px',opacity:1,transform:'translateY(0)'},{height:'0px',opacity:0,transform:'translateY(-7px)'}],{duration:260,easing:'cubic-bezier(.4,0,.2,1)'});group._checklistAnimation.onfinish=()=>{group.classList.add('recolhido');wrapper.style.removeProperty('transition');group._checklistAnimation=null}}
      else group.classList.add('recolhido')
    }else{
      group.classList.remove('recolhido');if(content)content.inert=false;
      const height=Math.max(wrapper?.scrollHeight||0,1);
      if(wrapper?.animate){wrapper.style.transition='none';group._checklistAnimation=wrapper.animate([{height:'0px',opacity:0,transform:'translateY(-7px)'},{height:height+'px',opacity:1,transform:'translateY(0)'}],{duration:280,easing:'cubic-bezier(.2,.8,.2,1)'});group._checklistAnimation.onfinish=()=>{wrapper.style.removeProperty('transition');group._checklistAnimation=null}}
    }
  }
  function ensureConfirmDialog(){
    let backdrop=$('app-confirm-backdrop');if(backdrop)return backdrop;
    backdrop=document.createElement('div');backdrop.id='app-confirm-backdrop';backdrop.className='app-confirm-backdrop';backdrop.setAttribute('aria-hidden','true');
    backdrop.innerHTML='<section class="app-confirm" role="alertdialog" aria-modal="true" aria-labelledby="app-confirm-title" aria-describedby="app-confirm-message"><div class="app-confirm-icon" aria-hidden="true">!</div><div><small>CONFIRMAÇÃO</small><h2 id="app-confirm-title">Confirmar ação</h2><p id="app-confirm-message"></p></div><footer><button class="btn" type="button" data-confirm-cancel>Cancelar</button><button class="btn danger" type="button" data-confirm-accept>Excluir</button></footer></section>';
    document.body.appendChild(backdrop);
    const finish=value=>{if(!backdrop.classList.contains('open'))return;backdrop.classList.remove('open');backdrop.setAttribute('aria-hidden','true');const resolve=state.confirmResolver;state.confirmResolver=null;resolve?.(value)};
    backdrop.querySelector('[data-confirm-cancel]').addEventListener('click',()=>finish(false));backdrop.querySelector('[data-confirm-accept]').addEventListener('click',()=>finish(true));backdrop.addEventListener('click',event=>{if(event.target===backdrop)finish(false)});
    document.addEventListener('keydown',event=>{if(event.key==='Escape'&&backdrop.classList.contains('open')){event.preventDefault();event.stopImmediatePropagation();finish(false)}},true);
    backdrop._finish=finish;return backdrop
  }
  function appConfirm(title,message,confirmLabel='Excluir'){
    const backdrop=ensureConfirmDialog();backdrop.querySelector('#app-confirm-title').textContent=title;backdrop.querySelector('#app-confirm-message').textContent=message;backdrop.querySelector('[data-confirm-accept]').textContent=confirmLabel;
    backdrop.classList.add('open');backdrop.setAttribute('aria-hidden','false');setTimeout(()=>backdrop.querySelector('[data-confirm-cancel]').focus(),20);
    return new Promise(resolve=>{if(state.confirmResolver)state.confirmResolver(false);state.confirmResolver=resolve})
  }
  async function deleteWork(){if(!canDeleteWork())return toast('Você não possui permissão para excluir esta obra.','error');const work=state.editing;if(!work)return;if(!await appConfirm('Excluir esta obra?','Esta ação não pode ser desfeita. Todos os dados e checklists vinculados serão removidos.'))return;setBusy(true);try{await rpc('obras_delete',{p_id:work.id,p_version:work.version});state.works=state.works.filter(w=>w.id!==work.id);closeModal();render();toast('Obra excluída.')}catch(error){toast(safeMessage(error,'Não foi possível excluir a obra.'),'error')}finally{setBusy(false)}}

  function timelineData(list){return (list||[]).filter(work=>work.status!=='concluida').map(work=>{const groups=(work.checklist_groups||[]).map(group=>{const items=(group.items||[]).filter(item=>item.start_date&&item.end_date);return{...group,items}}).filter(group=>group.items.length&&derivedPeriod(group.items).start&&derivedPeriod(group.items).end);return{...work,checklist_groups:groups}}).filter(work=>work.checklist_groups.length)}
  function timelineRange(){const anchor=isoDate(state.timelineAnchor||new Date()),start=addIsoDays(anchor,-45);return{start,end:addIsoDays(start,TIMELINE_DAYS-1),days:TIMELINE_DAYS}}
  function shiftTimelineMonth(amount){state.timelineAnchor=new Date(state.timelineAnchor.getFullYear(),state.timelineAnchor.getMonth()+amount,1);state.timelineInitialized=true;state.timelineAutoCenter=true;renderTimeline(filtered(),false)}
  function timelineBar(period,range,options={}){
    if(!period.start||!period.end)return '';const start=period.start,end=period.end;if(end<range.start||start>range.end)return '';
    const visibleStart=start<range.start?range.start:start,visibleEnd=end>range.end?range.end:end,left=isoDayDiff(range.start,visibleStart)*TIMELINE_DAY+3,width=Math.max(12,(isoDayDiff(visibleStart,visibleEnd)+1)*TIMELINE_DAY-6),editable=options.kind==='item'&&canEditWork(),label=options.label||'Período',tooltip=label+' · '+dateLabel(start)+' até '+dateLabel(end);
    return '<div class="gantt-bar '+esc(options.className||'')+(editable?' editable':' readonly')+'" style="left:'+left+'px;width:'+width+'px;--bar-color:'+esc(options.color||'var(--accent)')+'" data-work-id="'+esc(options.workId||'')+'" data-group-id="'+esc(options.groupId||'')+'" data-item-id="'+esc(options.itemId||'')+'" data-start="'+esc(start)+'" data-end="'+esc(end)+'" data-tooltip="'+esc(tooltip)+'" aria-label="'+esc(tooltip)+'" tabindex="0">'+(editable?'<i class="gantt-resize start" data-resize="start" aria-hidden="true"></i>':'')+'<span>'+esc(label)+'</span>'+(editable?'<i class="gantt-resize end" data-resize="end" aria-hidden="true"></i>':'')+'</div>'
  }
  function timelineRow(label,className,bar,attributes=''){return '<div class="gantt-label '+className+'" '+attributes+'>'+label+'</div><div class="gantt-track '+className+'" '+attributes+'>'+bar+'</div>'}
  function animateTimelineBranch(kind,id,list){
    const root=$('timeline');if(root.dataset.animating==='true')return;const set=kind==='work'?state.timelineCollapsed:state.timelineGroupCollapsed,expanded=!set.has(id),attribute=kind==='work'?'data-parent-work':'data-parent-group',selector='['+attribute+'="'+id+'"]';root.dataset.animating='true';
    if(expanded){const rows=[...root.querySelectorAll(selector)];rows.forEach(row=>row.animate([{opacity:1,transform:'translateY(0)'},{opacity:0,transform:'translateY(-8px)'}],{duration:170,easing:'ease-in',fill:'forwards'}));setTimeout(()=>{set.add(id);renderTimeline(list,true);root.dataset.animating='false'},165)}
    else{set.delete(id);renderTimeline(list,true);requestAnimationFrame(()=>{[...root.querySelectorAll(selector)].forEach(row=>row.animate([{opacity:0,transform:'translateY(-8px)'},{opacity:1,transform:'translateY(0)'}],{duration:220,easing:'cubic-bezier(.2,.8,.2,1)'}));setTimeout(()=>root.dataset.animating='false',225)})}
  }
  function renderTimeline(list,keepScroll=false){
    const root=$('timeline');if(!root)return;const previous=root.querySelector('.gantt-grid'),scrollLeft=keepScroll&&previous?previous.scrollLeft:0,scrollTop=keepScroll&&previous?previous.scrollTop:0,data=timelineData(list),range=timelineRange(),startDate=parseIsoDate(range.start),endDate=parseIsoDate(range.end);
    $('timeline-label').textContent=new Intl.DateTimeFormat('pt-BR',{month:'short',year:'numeric'}).format(startDate)+' — '+new Intl.DateTimeFormat('pt-BR',{month:'short',year:'numeric'}).format(endDate);
    let rows='';sortWorks(data).forEach(work=>{const workCollapsed=state.timelineCollapsed.has(work.id),color=timelineColor(work.id),workPeriod=workTimelinePeriod(work),workAttr='data-work-row="'+esc(work.id)+'"';rows+=timelineRow('<button class="gantt-toggle" type="button" data-timeline-work="'+esc(work.id)+'" aria-expanded="'+String(!workCollapsed)+'"><span>▾</span><strong>'+esc(work.title)+'</strong></button>','level-work',timelineBar(workPeriod,range,{className:'work-period',color,label:work.title,workId:work.id}),workAttr);if(workCollapsed)return;work.checklist_groups.forEach(group=>{const groupCollapsed=state.timelineGroupCollapsed.has(group.id),period=derivedPeriod(group.items),groupAttr='data-parent-work="'+esc(work.id)+'" data-row-group="'+esc(group.id)+'"';rows+=timelineRow('<button class="gantt-toggle group" type="button" data-timeline-group="'+esc(group.id)+'" aria-expanded="'+String(!groupCollapsed)+'"><span>▾</span><strong>'+esc(group.title)+'</strong></button>','level-group',timelineBar(period,range,{className:'group-period',color,label:group.title,workId:work.id,groupId:group.id}),groupAttr);if(groupCollapsed)return;group.items.forEach(item=>{const itemLabel=item.title||item.text||'Tarefa',itemAttr='data-parent-work="'+esc(work.id)+'" data-parent-group="'+esc(group.id)+'" data-row-item="'+esc(item.id||'')+'"';rows+=timelineRow('<div class="gantt-item-label '+(item.done?'done':'')+'"><span></span><strong>'+esc(itemLabel)+'</strong></div>','level-item',timelineBar({start:item.start_date,end:item.end_date},range,{kind:'item',className:'item-period '+(item.done?'done':''),color,label:itemLabel,workId:work.id,groupId:group.id,itemId:item.id}),itemAttr)})})});
    root.innerHTML=data.length?'<div class="gantt-grid" style="--timeline-days:'+range.days+';--timeline-width:'+(range.days*TIMELINE_DAY)+'px" data-range-start="'+range.start+'" data-range-days="'+range.days+'"><div class="gantt-corner">Obras e etapas</div><div class="gantt-header">'+timelineHeader(range)+'</div>'+rows+'</div>':'<div class="timeline-empty"><strong>Nenhuma etapa programada</strong><span>Defina início e conclusão nas tarefas de uma obra não concluída.</span></div>';
    const grid=root.querySelector('.gantt-grid');if(!grid)return;if(state.timelineAutoCenter){const todayIndex=isoDayDiff(range.start,isoDate(new Date())),labelWidth=Math.min(340,Math.max(190,grid.clientWidth*.24));grid.scrollLeft=Math.max(0,labelWidth+todayIndex*TIMELINE_DAY-(grid.clientWidth-labelWidth)/2);state.timelineAutoCenter=false}else grid.scrollLeft=scrollLeft;grid.scrollTop=scrollTop;
    root.querySelectorAll('[data-timeline-work]').forEach(button=>button.addEventListener('click',()=>animateTimelineBranch('work',button.dataset.timelineWork,data)));root.querySelectorAll('[data-timeline-group]').forEach(button=>button.addEventListener('click',()=>animateTimelineBranch('group',button.dataset.timelineGroup,data)));bindTimelineBars(root,range)
  }
  function showView(view){
    state.view=view;if(view==='cronograma'){state.timelineAnchor=new Date();state.timelineInitialized=true;state.timelineAutoCenter=true}
    document.querySelectorAll('[data-view]').forEach(btn=>btn.classList.toggle('active',btn.dataset.view===view));document.querySelectorAll('.view').forEach(el=>el.classList.toggle('active',el.id==='view-'+view));$('page-title').textContent=VIEW_META[view][0];$('page-subtitle').textContent=VIEW_META[view][1];$('new-work-btn').hidden=view!=='painel'||!canCreateWork();toggleAside(false);render();if(view==='locais')setTimeout(()=>state.map?.invalidateSize(),80)
  }
  document.addEventListener('click',event=>{const status=$('status-filter');if(status?.open&&!event.target.closest('#status-filter'))status.open=false});

  /* Obras V25: cronograma contínuo, salto temporal e PDF */
  const TIMELINE_INITIAL_DAYS=1464,TIMELINE_CHUNK_DAYS=732,TIMELINE_MAX_JOURNEY_DAYS=7320,TIMELINE_JOURNEY_BUFFER_DAYS=180;
  function ensureTimelineWindow(target){const base=target instanceof Date?target:new Date();if(!state.timelineWindowStart){state.timelineWindowStart=isoDate(new Date(base.getFullYear()-1,0,1,12));state.timelineWindowDays=TIMELINE_INITIAL_DAYS}}
  function timelineRange(){ensureTimelineWindow(state.timelineAnchor||new Date());return{start:state.timelineWindowStart,end:addIsoDays(state.timelineWindowStart,state.timelineWindowDays-1),days:state.timelineWindowDays}}
  function timelineRenderedRange(grid){const start=grid?.dataset?.rangeStart,days=Number(grid?.dataset?.rangeDays);return start&&Number.isFinite(days)&&days>0?{start,end:addIsoDays(start,days-1),days}:timelineRange()}
  function visibleTimelineDate(grid,range){const labelWidth=Math.min(340,Math.max(190,grid.clientWidth*.24)),day=Math.max(0,Math.round((grid.scrollLeft-labelWidth+(grid.clientWidth-labelWidth)/2)/TIMELINE_DAY));return parseIsoDate(addIsoDays(range.start,day))||new Date()}
  function updateTimelineVisibleLabel(grid,range){$('timeline-label').textContent=new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(visibleTimelineDate(grid,range))}
  function timelineTargetLeft(grid,range,targetIso){const labelWidth=Math.min(340,Math.max(190,grid.clientWidth*.24));return Math.max(0,labelWidth+isoDayDiff(range.start,targetIso)*TIMELINE_DAY-(grid.clientWidth-labelWidth)/2)}
  function cancelTimelineScrollAnimation(){if(state.timelineScrollRaf)cancelAnimationFrame(state.timelineScrollRaf);if(state.timelineWarpAnimation){try{state.timelineWarpAnimation.cancel()}catch(_){}}state.timelineScrollRaf=null;state.timelineWarpAnimation=null;state.timelineWarping=false;state.timelineScrollGeneration=(state.timelineScrollGeneration||0)+1}
  function animateTimelineScroll(grid,target,duration=920){cancelTimelineScrollAnimation();const generation=state.timelineScrollGeneration,maxLeft=Math.max(0,grid.scrollWidth-grid.clientWidth),start=grid.scrollLeft,destination=Math.min(maxLeft,Math.max(0,target)),delta=destination-start,started=performance.now();if(Math.abs(delta)<1){grid.scrollLeft=destination;return}const step=now=>{if(generation!==state.timelineScrollGeneration)return;if(!grid.isConnected){state.timelineScrollRaf=null;return}const t=Math.min(1,(now-started)/duration),eased=t<.5?16*t*t*t*t*t:1-Math.pow(-2*t+2,5)/2;grid.scrollLeft=start+delta*eased;if(t<1)state.timelineScrollRaf=requestAnimationFrame(step);else{grid.scrollLeft=destination;state.timelineScrollRaf=null}};state.timelineScrollRaf=requestAnimationFrame(step)}
  function ensureTimelineDateInWindow(targetIso,sourceIso){const range=timelineRange();if(targetIso>=range.start&&targetIso<=range.end)return{changed:false,far:false};const distance=sourceIso?Math.abs(isoDayDiff(sourceIso,targetIso)):Infinity;if(sourceIso&&distance<=TIMELINE_MAX_JOURNEY_DAYS){const first=sourceIso<targetIso?sourceIso:targetIso,last=sourceIso<targetIso?targetIso:sourceIso,start=addIsoDays(first,-TIMELINE_JOURNEY_BUFFER_DAYS),end=addIsoDays(last,TIMELINE_JOURNEY_BUFFER_DAYS);state.timelineWindowStart=start;state.timelineWindowDays=Math.max(TIMELINE_INITIAL_DAYS,isoDayDiff(start,end)+1);return{changed:true,far:false}}const target=parseIsoDate(targetIso);state.timelineWindowStart=isoDate(new Date(target.getFullYear()-1,0,1,12));state.timelineWindowDays=TIMELINE_INITIAL_DAYS;return{changed:true,far:true}}
  function timelineJourneyDuration(sourceIso,targetIso){return Math.min(1800,Math.max(760,600+Math.log1p(Math.abs(isoDayDiff(sourceIso,targetIso)))*90))}
  function animateTimelineFarJump(grid,targetIso,sourceIso,scrollTop){const generation=state.timelineScrollGeneration,direction=!sourceIso||targetIso>=sourceIso?1:-1,finish=()=>{if(generation!==state.timelineScrollGeneration)return;state.timelineWarpAnimation=null;state.timelineJumpDate=targetIso;state.timelineJumpAnimate=false;state.timelineJumpFromDate=null;state.timelineJumpFromTop=scrollTop;renderTimeline(filtered(),false);const next=$('timeline')?.querySelector('.gantt-grid');if(!next?.animate){state.timelineWarping=false;return}const incoming=next.animate([{opacity:.16,transform:'translateX('+(direction*48)+'px) scale(.985)',filter:'blur(1.5px)'},{opacity:1,transform:'translateX(0) scale(1)',filter:'blur(0)'}],{duration:420,easing:'cubic-bezier(0,0,.2,1)',fill:'both'});state.timelineWarpAnimation=incoming;incoming.finished.catch(()=>{}).finally(()=>{if(generation===state.timelineScrollGeneration){state.timelineWarpAnimation=null;state.timelineWarping=false}})};state.timelineWarping=true;if(!grid?.animate||window.matchMedia?.('(prefers-reduced-motion: reduce)')?.matches){finish();return}const outgoing=grid.animate([{opacity:1,transform:'translateX(0) scale(1)',filter:'blur(0)'},{opacity:.16,transform:'translateX('+(-direction*48)+'px) scale(.985)',filter:'blur(1.5px)'}],{duration:280,easing:'cubic-bezier(.4,0,1,1)',fill:'forwards'});state.timelineWarpAnimation=outgoing;outgoing.finished.then(finish).catch(()=>{if(generation===state.timelineScrollGeneration)finish()})}
  function goToTimelineDate(target,animate=true){cancelTimelineScrollAnimation();const targetIso=isoDate(target),currentGrid=$('timeline')?.querySelector('.gantt-grid'),currentRange=timelineRenderedRange(currentGrid);if(currentGrid){state.timelineWindowStart=currentRange.start;state.timelineWindowDays=currentRange.days}const sourceIso=currentGrid?isoDate(visibleTimelineDate(currentGrid,currentRange)):isoDate(state.timelineAnchor||new Date()),currentTop=currentGrid?currentGrid.scrollTop:0,journey=ensureTimelineDateInWindow(targetIso,sourceIso),duration=timelineJourneyDuration(sourceIso,targetIso);state.timelineJumpDate=null;state.timelineJumpAnimate=false;state.timelineJumpFromDate=null;state.timelineJumpFromTop=null;state.timelineAutoCenter=false;if(!journey.changed){if(!currentGrid)return;const left=timelineTargetLeft(currentGrid,currentRange,targetIso);if(animate)animateTimelineScroll(currentGrid,left,duration);else currentGrid.scrollLeft=left;return}if(animate&&journey.far){animateTimelineFarJump(currentGrid,targetIso,sourceIso,currentTop);return}state.timelineJumpDate=targetIso;state.timelineJumpAnimate=animate;state.timelineJumpFromDate=animate?sourceIso:null;state.timelineJumpFromTop=currentTop;state.timelineJumpDuration=duration;renderTimeline(filtered(),false)}
  function shiftTimelineMonth(amount){const grid=$('timeline')?.querySelector('.gantt-grid'),range=timelineRange(),current=grid?visibleTimelineDate(grid,range):(state.timelineAnchor||new Date());goToTimelineDate(new Date(current.getFullYear(),current.getMonth()+amount,1,12),true)}
  function expandTimelineWindow(direction,grid){if(state.timelineExpanding)return;state.timelineExpanding=true;const oldLeft=grid.scrollLeft,oldTop=grid.scrollTop,shift=direction<0?-TIMELINE_CHUNK_DAYS:TIMELINE_CHUNK_DAYS;state.timelineWindowStart=addIsoDays(state.timelineWindowStart,shift);state.timelineWindowDays=TIMELINE_INITIAL_DAYS;state.timelineRestoreLeft=Math.max(0,oldLeft-shift*TIMELINE_DAY);state.timelineRestoreTop=oldTop;requestAnimationFrame(()=>{renderTimeline(filtered(),false);requestAnimationFrame(()=>{state.timelineExpanding=false})})}
  function captureTimelineViewport(){const grid=$('timeline')?.querySelector('.gantt-grid');return grid?{grid,left:grid.scrollLeft,top:grid.scrollTop,windowX:window.scrollX,windowY:window.scrollY}:null}
   function restoreTimelineViewport(viewport){if(!viewport)return;const grid=viewport.grid?.isConnected?viewport.grid:$('timeline')?.querySelector('.gantt-grid');if(grid){grid.scrollLeft=viewport.left;grid.scrollTop=viewport.top}window.scrollTo({left:viewport.windowX,top:viewport.windowY,behavior:'auto'})}
   function openTimelineChecklistItem(workId,groupId,itemId){
    if(!canEditWork()||!itemId)return;const work=state.works.find(item=>String(item.id)===String(workId));if(!work)return;const viewport=captureTimelineViewport();openModal(work,{groupId,itemId});requestAnimationFrame(()=>restoreTimelineViewport(viewport));setTimeout(()=>restoreTimelineViewport(viewport),80)
  }
  function bindTimelineRowEditors(root){
    if(!canEditWork())return;
    root.querySelectorAll('[data-row-item]').forEach(row=>{row.classList.add('timeline-row-editable');row.addEventListener('click',event=>{if(event.target.closest('button,.gantt-bar'))return;openTimelineChecklistItem(row.dataset.parentWork,row.dataset.parentGroup,row.dataset.rowItem)})})
  }
  function bindInfiniteTimeline(grid,range){let ticking=false;grid.addEventListener('scroll',()=>{if(ticking)return;ticking=true;requestAnimationFrame(()=>{ticking=false;updateTimelineVisibleLabel(grid,range);if(state.timelineScrollRaf||state.timelineWarping||state.timelineSaving||state.timelineExpanding)return;const edge=Math.min(320,grid.clientWidth*.22);if(grid.scrollLeft<edge)expandTimelineWindow(-1,grid);else if(grid.scrollLeft+grid.clientWidth>grid.scrollWidth-edge)expandTimelineWindow(1,grid)})},{passive:true})}
  function renderTimeline(list,keepScroll=false){const root=$('timeline');if(!root)return;const previous=root.querySelector('.gantt-grid'),scrollLeft=keepScroll&&previous?previous.scrollLeft:0,scrollTop=keepScroll&&previous?previous.scrollTop:0;if(state.timelineAutoCenter)ensureTimelineDateInWindow(isoDate(new Date()));const data=timelineData(list),range=timelineRange();let rows='';sortWorks(data).forEach(work=>{const workCollapsed=state.timelineCollapsed.has(work.id),color=timelineColor(work.id),workPeriod=workTimelinePeriod(work),workAttr='data-work-row="'+esc(work.id)+'"';rows+=timelineRow('<button class="gantt-toggle" type="button" data-timeline-work="'+esc(work.id)+'" aria-expanded="'+String(!workCollapsed)+'"><span>▾</span><strong>'+esc(work.title)+'</strong></button>','level-work',timelineBar(workPeriod,range,{className:'work-period',color,label:work.title,workId:work.id}),workAttr);if(workCollapsed)return;work.checklist_groups.forEach(group=>{const groupCollapsed=state.timelineGroupCollapsed.has(group.id),period=derivedPeriod(group.items),groupAttr='data-parent-work="'+esc(work.id)+'" data-row-group="'+esc(group.id)+'"';rows+=timelineRow('<button class="gantt-toggle group" type="button" data-timeline-group="'+esc(group.id)+'" aria-expanded="'+String(!groupCollapsed)+'"><span>▾</span><strong>'+esc(group.title)+'</strong></button>','level-group',timelineBar(period,range,{className:'group-period',color,label:group.title,workId:work.id,groupId:group.id}),groupAttr);if(groupCollapsed)return;group.items.forEach(item=>{const itemLabel=item.title||item.text||'Tarefa',itemAttr='data-parent-work="'+esc(work.id)+'" data-parent-group="'+esc(group.id)+'" data-row-item="'+esc(item.id||'')+'"';rows+=timelineRow('<div class="gantt-item-label '+(item.done?'done':'')+'"><span></span><strong>'+esc(itemLabel)+'</strong></div>','level-item',timelineBar({start:item.start_date,end:item.end_date},range,{kind:'item',className:'item-period '+(item.done?'done':''),color,label:itemLabel,workId:work.id,groupId:group.id,itemId:item.id}),itemAttr)})})});root.innerHTML=data.length?'<div class="gantt-grid" style="--timeline-days:'+range.days+';--timeline-width:'+(range.days*TIMELINE_DAY)+'px" data-range-start="'+range.start+'" data-range-days="'+range.days+'"><div class="gantt-corner">Obras e etapas</div><div class="gantt-header">'+timelineHeader(range)+'</div>'+rows+'</div>':'<div class="timeline-empty"><strong>Nenhuma etapa programada</strong><span>Defina início e conclusão nas tarefas de uma obra não concluída.</span></div>';const grid=root.querySelector('.gantt-grid');if(!grid)return;if(state.timelineRestoreLeft!=null){grid.scrollLeft=state.timelineRestoreLeft;grid.scrollTop=state.timelineRestoreTop||0;state.timelineRestoreLeft=null;state.timelineRestoreTop=null}else if(state.timelineJumpDate){const left=timelineTargetLeft(grid,range,state.timelineJumpDate);grid.scrollLeft=state.timelineJumpFromDate?timelineTargetLeft(grid,range,state.timelineJumpFromDate):scrollLeft;grid.scrollTop=state.timelineJumpFromTop!=null?state.timelineJumpFromTop:scrollTop;if(state.timelineJumpAnimate)animateTimelineScroll(grid,left,state.timelineJumpDuration||920);else grid.scrollLeft=left;state.timelineJumpDate=null;state.timelineJumpAnimate=false;state.timelineJumpFromDate=null;state.timelineJumpFromLeft=null;state.timelineJumpFromTop=null;state.timelineJumpDuration=null}else if(state.timelineAutoCenter){grid.scrollLeft=timelineTargetLeft(grid,range,isoDate(new Date()));state.timelineAutoCenter=false}else{grid.scrollLeft=scrollLeft;grid.scrollTop=scrollTop}updateTimelineVisibleLabel(grid,range);bindInfiniteTimeline(grid,range);root.querySelectorAll('[data-timeline-work]').forEach(button=>button.addEventListener('click',()=>animateTimelineBranch('work',button.dataset.timelineWork,data)));root.querySelectorAll('[data-timeline-group]').forEach(button=>button.addEventListener('click',()=>animateTimelineBranch('group',button.dataset.timelineGroup,data)));bindTimelineBars(root,range);bindTimelineRowEditors(root)}
  function closeTimelineJump(){const panel=$('timeline-jump-popover');if(!panel)return;window.__obrasCloseTimelineMonth?.();panel.hidden=true;panel.setAttribute('aria-hidden','true');$('timeline-jump-btn')?.classList.remove('active')}
  function openTimelineJump(){const panel=$('timeline-jump-popover'),button=$('timeline-jump-btn'),grid=$('timeline')?.querySelector('.gantt-grid'),date=grid?visibleTimelineDate(grid,timelineRange()):new Date();window.__obrasSetTimelineMonth?.(date.getMonth());window.__obrasCloseTimelineMonth?.();$('timeline-jump-year').value=String(date.getFullYear());const rect=button.getBoundingClientRect();panel.hidden=false;panel.setAttribute('aria-hidden','false');button.classList.add('active');panel.style.left=Math.max(10,Math.min(rect.left,window.innerWidth-310))+'px';panel.style.top=(rect.bottom+7)+'px'}
  function eligibleTimelinePdfWorks(){return state.works.filter(work=>workVisibleToCurrentRole(work)&&work.status!=='concluida')}
  function openTimelinePdf(){const works=eligibleTimelinePdfWorks(),list=$('timeline-pdf-list');list.innerHTML=works.length?sortWorks(works).map(work=>'<label><input type="checkbox" value="'+esc(work.id)+'" checked><span><strong>'+esc(work.title)+'</strong><small>'+esc(work.city||work.franchise_name||'Obra em andamento')+'</small></span></label>').join(''):'<p class="empty-state">Nenhuma obra não concluída disponível.</p>';$('timeline-pdf-backdrop').classList.add('open');$('timeline-pdf-backdrop').setAttribute('aria-hidden','false')}
  function closeTimelinePdf(){$('timeline-pdf-backdrop').classList.remove('open');$('timeline-pdf-backdrop').setAttribute('aria-hidden','true')}
  function selectedTimelinePdfWorks(){const ids=new Set([...$('timeline-pdf-list').querySelectorAll('input:checked')].map(input=>input.value));return eligibleTimelinePdfWorks().filter(work=>ids.has(work.id))}
  function hexRgb(value){const clean=String(value||'').replace('#',''),n=parseInt(clean.length===3?clean.split('').map(c=>c+c).join(''):clean,16);return Number.isFinite(n)?[(n>>16)&255,(n>>8)&255,n&255]:[239,108,26]}
  function timelinePdfRows(work){const rows=[{type:'work',label:work.title,period:workTimelinePeriod(work)}];(work.checklist_groups||[]).forEach(group=>{const items=(group.items||[]).filter(item=>item.start_date&&item.end_date);if(!items.length)return;rows.push({type:'group',label:group.title,period:derivedPeriod(items)});items.forEach(item=>rows.push({type:'item',label:item.title||item.text||'Tarefa',period:{start:item.start_date,end:item.end_date},done:item.done}))});return rows}
  let obrasPdfFontsPromise=null,obrasPdfFontName='Montserrat';
  function arrayBufferToBase64(buffer){
    const bytes=new Uint8Array(buffer),chunk=0x8000;
    let binary='';
    for(let index=0;index<bytes.length;index+=chunk)binary+=String.fromCharCode(...bytes.subarray(index,index+chunk));
    return btoa(binary);
  }
  async function registerObrasPdfFonts(doc){
    if(!obrasPdfFontsPromise){
      obrasPdfFontsPromise=Promise.all([
        fetch('../shared/Montserrat-Variable.ttf',{credentials:'same-origin',cache:'force-cache'}),
        fetch('../shared/Montserrat-Bold.ttf',{credentials:'same-origin',cache:'force-cache'})
      ]).then(async responses=>{
        if(responses.some(response=>!response.ok))throw new Error('Não foi possível carregar as fontes do PDF.');
        const buffers=await Promise.all(responses.map(response=>response.arrayBuffer()));
        return buffers.map(arrayBufferToBase64);
      });
    }
    const [regular,bold]=await obrasPdfFontsPromise;
    doc.addFileToVFS('Montserrat-Regular.ttf',regular);
    doc.addFont('Montserrat-Regular.ttf','Montserrat','normal');
    doc.addFileToVFS('Montserrat-Bold.ttf',bold);
    doc.addFont('Montserrat-Bold.ttf','Montserrat','bold');
  }
  async function buildTimelinePdf(works){
    const PDF=window.jspdf&&window.jspdf.jsPDF;
    if(!PDF)throw new Error('Gerador de PDF indisponível.');
    if(!works.length)throw new Error('Selecione ao menos uma obra.');
    const entries=works
      .map(work=>({work,rows:timelinePdfRows(work)}))
      .filter(entry=>entry.rows.some(row=>row.period?.start&&row.period?.end));
    if(!entries.length)throw new Error('As obras selecionadas não possuem atividades com início e fim definidos.');

    const doc=new PDF({orientation:'landscape',unit:'mm',format:'a4'});
    obrasPdfFontName='Montserrat';
    try{await registerObrasPdfFonts(doc)}catch(error){console.warn('[Obras PDF] Fonte local indisponível; usando fonte segura padrão.',error);obrasPdfFontName='helvetica'}
    const pageW=297,pageH=210,left=9,labelW=68,chartX=77,chartW=211;
    const daysPerPage=28,rowH=8,rowsPerPage=16;
    let firstPage=true;

    entries.forEach(entry=>{
      const dated=entry.rows.filter(row=>row.period?.start&&row.period?.end);
      const dates=dated.flatMap(row=>[row.period.start,row.period.end]).sort();
      const firstIso=dates[0],lastIso=dates[dates.length-1];
      const totalDays=isoDayDiff(firstIso,lastIso)+1;
      const color=hexRgb(timelineColor(entry.work.id));

      for(let dayOffset=0;dayOffset<totalDays;dayOffset+=daysPerPage){
        const segmentStart=addIsoDays(firstIso,dayOffset);
        const segmentDays=Math.min(daysPerPage,totalDays-dayOffset);
        const segmentEnd=addIsoDays(segmentStart,segmentDays-1);

        for(let rowOffset=0;rowOffset<dated.length;rowOffset+=rowsPerPage){
          if(!firstPage)doc.addPage('a4','landscape');
          firstPage=false;
          const pageRows=dated.slice(rowOffset,rowOffset+rowsPerPage);
          const dayW=chartW/daysPerPage,top=32,dayTop=top+8;

          doc.setFillColor(28,31,36);
          doc.rect(0,0,pageW,25,'F');
          doc.setTextColor(255,255,255);
          doc.setFont(obrasPdfFontName,'bold');
          doc.setFontSize(15);
          doc.text('Cronograma de Obras',left,11);
          doc.setFontSize(9);
          doc.text(String(entry.work.title||'Obra'),left,18);
          doc.setTextColor(190,195,202);
          doc.setFontSize(7);
          doc.text(dateLabel(segmentStart)+' - '+dateLabel(segmentEnd),pageW-9,16,{align:'right'});

          doc.setFillColor(238,240,242);
          doc.rect(left,top,labelW,18,'F');
          doc.setTextColor(70,77,87);
          doc.setFontSize(7);
          doc.text('OBRAS E ETAPAS',left+3,top+11);

          let monthIndex=0;
          while(monthIndex<segmentDays){
            const current=parseIsoDate(addIsoDays(segmentStart,monthIndex));
            let monthEnd=monthIndex;
            while(monthEnd+1<segmentDays){
              const next=parseIsoDate(addIsoDays(segmentStart,monthEnd+1));
              if(next.getMonth()!==current.getMonth()||next.getFullYear()!==current.getFullYear())break;
              monthEnd++;
            }
            const x=chartX+monthIndex*dayW,width=(monthEnd-monthIndex+1)*dayW;
            doc.setFillColor(229,232,236);doc.setDrawColor(210,214,220);doc.rect(x,top,width,8,'FD');
            doc.setTextColor(62,68,77);doc.setFont(obrasPdfFontName,'bold');doc.setFontSize(6.2);
            const label=current.toLocaleDateString('pt-BR',{month:'long',year:'numeric'}).toLocaleUpperCase('pt-BR');
            doc.text(label,x+width/2,top+5.3,{align:'center',maxWidth:Math.max(2,width-1)});
            monthIndex=monthEnd+1;
          }
          for(let dayIndex=0;dayIndex<daysPerPage;dayIndex++){
            const x=chartX+dayIndex*dayW;
            const date=parseIsoDate(addIsoDays(segmentStart,dayIndex));
            const weekend=date.getDay()===0||date.getDay()===6;
            doc.setFillColor(weekend?245:250,247,249);
            doc.rect(x,dayTop,dayW,10,'F');
            doc.setDrawColor(224,227,231);
            doc.rect(x,dayTop,dayW,10);
            if(dayIndex<segmentDays){
              doc.setTextColor(80,88,98);
              doc.setFontSize(6);
              doc.text(String(date.getDate()),x+dayW/2,dayTop+6,{align:'center'});
            }
          }

          pageRows.forEach((row,index)=>{
            const y=dayTop+10+index*rowH;
            const shade=row.type==='work'?[235,237,240]:row.type==='group'?[244,245,247]:[251,251,252];
            doc.setFillColor(...shade);
            doc.rect(left,y,labelW,rowH,'F');
            doc.setDrawColor(228,230,233);
            doc.rect(left,y,labelW,rowH);
            doc.setFont(obrasPdfFontName,row.type==='item'?'normal':'bold');
            doc.setFontSize(row.type==='work'?7.4:row.type==='group'?6.8:6.5);
            doc.setTextColor(45,49,56);
            const indent=row.type==='item'?7:row.type==='group'?4:2;
            const shortLabel=doc.splitTextToSize(String(row.label||''),labelW-indent-2)[0]||'';
            doc.text(shortLabel,left+indent,y+5.2);

            for(let dayIndex=0;dayIndex<daysPerPage;dayIndex++){
              const x=chartX+dayIndex*dayW;
              const alternate=dayIndex%2===1;
              doc.setFillColor(alternate?252:248,alternate?252:249,alternate?253:250);
              doc.rect(x,y,dayW,rowH,'F');
              doc.setDrawColor(235,237,239);
              doc.rect(x,y,dayW,rowH);
            }

            const visibleStart=row.period.start<segmentStart?segmentStart:row.period.start;
            const visibleEnd=row.period.end>segmentEnd?segmentEnd:row.period.end;
            if(visibleStart<=visibleEnd){
              const barX=chartX+isoDayDiff(segmentStart,visibleStart)*dayW+.7;
              const barW=Math.max(2,(isoDayDiff(visibleStart,visibleEnd)+1)*dayW-1.4);
              doc.setFillColor(...color);
              doc.roundedRect(barX,y+2,barW,rowH-4,1,1,'F');
            }
          });
        }
      }
    });

    const pages=doc.getNumberOfPages();
    for(let page=1;page<=pages;page++){
      doc.setPage(page);
      doc.setFont(obrasPdfFontName,'normal');
      doc.setFontSize(6.5);
      doc.setTextColor(110,116,124);
      doc.text('321 Modular | Acompanhamento de Obras',left,pageH-6);
      doc.text(page+' / '+pages,pageW-9,pageH-6,{align:'right'});
    }
    doc.setProperties({
      title:'Cronograma de Obras - 321 Modular',
      subject:'Cronograma das obras selecionadas',
      creator:'321 Modular SuperApp'
    });
    return doc;
  }  let obrasPdfPreviewUrl=null,obrasPdfPreviewDoc=null,obrasPdfPreviewName='cronograma-de-obras.pdf';
  function closeObrasPdfPreview(){$('obras-pdf-preview-backdrop').classList.remove('open');$('obras-pdf-preview-backdrop').setAttribute('aria-hidden','true');$('obras-pdf-preview-frame').removeAttribute('src');$('obras-pdf-open').removeAttribute('href');if(obrasPdfPreviewUrl){URL.revokeObjectURL(obrasPdfPreviewUrl);obrasPdfPreviewUrl=null}obrasPdfPreviewDoc=null}
  function showObrasPdfPreview(doc){closeObrasPdfPreview();const blob=doc&&doc.output?doc.output('blob'):null;if(!blob)throw new Error('PDF indisponível para pré-visualização.');obrasPdfPreviewDoc=doc;obrasPdfPreviewUrl=URL.createObjectURL(blob);const frame=$('obras-pdf-preview-frame'),open=$('obras-pdf-open');frame.title=obrasPdfPreviewName;frame.src=obrasPdfPreviewUrl;open.href=obrasPdfPreviewUrl;open.setAttribute('aria-label','Abrir '+obrasPdfPreviewName);$('obras-pdf-save').title='Salvar '+obrasPdfPreviewName;$('obras-pdf-preview-backdrop').classList.add('open');$('obras-pdf-preview-backdrop').setAttribute('aria-hidden','false')}
  async function createTimelinePdf(action){
    const buttons=[$('timeline-pdf-preview-btn'),$('timeline-pdf-download-btn')].filter(Boolean);
    buttons.forEach(button=>button.disabled=true);
    try{
      toast('Preparando PDF...');
      const doc=await buildTimelinePdf(selectedTimelinePdfWorks());
      closeTimelinePdf();
      if(action==='preview')showObrasPdfPreview(doc);
      else{doc.save(obrasPdfPreviewName);toast('PDF do Cronograma gerado.')}
    }catch(error){
      toast(error.message||'Não foi possível gerar o PDF.','error');
    }finally{
      buttons.forEach(button=>button.disabled=false);
    }
  }
  function configureTimelineV25(){
    const month=$('timeline-jump-month'),monthToggle=$('timeline-month-toggle'),monthMenu=$('timeline-month-menu'),year=$('timeline-jump-year');
    const monthNames=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];
    const setMonth=value=>{const parsed=Math.min(11,Math.max(0,Number(value)||0));month.value=String(parsed);$('timeline-month-label').textContent=monthNames[parsed];monthMenu.querySelectorAll('[data-timeline-month]').forEach(option=>{const selected=Number(option.dataset.timelineMonth)===parsed;option.classList.toggle('selected',selected);option.setAttribute('aria-selected',String(selected))})};
    const closeMonthMenu=()=>{monthMenu.hidden=true;monthToggle.classList.remove('open');monthToggle.setAttribute('aria-expanded','false')};
    const toggleMonthMenu=event=>{event.preventDefault();event.stopPropagation();const opening=monthMenu.hidden;closeMonthMenu();if(opening){monthMenu.hidden=false;monthToggle.classList.add('open');monthToggle.setAttribute('aria-expanded','true')}};
    monthToggle.addEventListener('click',toggleMonthMenu);
    monthMenu.querySelectorAll('[data-timeline-month]').forEach(option=>option.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();setMonth(option.dataset.timelineMonth);closeMonthMenu();monthToggle.focus()}));
    year.addEventListener('input',()=>{year.value=year.value.replace(/\D/g,'').slice(0,4)});
    document.querySelectorAll('[data-timeline-year-step]').forEach(button=>button.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();const current=Math.min(2300,Math.max(1900,Number(year.value)||new Date().getFullYear()));year.value=String(Math.min(2300,Math.max(1900,current+Number(button.dataset.timelineYearStep))))}));
    $('timeline-jump-btn').addEventListener('click',event=>{event.preventDefault();event.stopPropagation();$('timeline-jump-popover').hidden?openTimelineJump():closeTimelineJump()});
    $('timeline-jump-cancel').addEventListener('click',closeTimelineJump);
    $('timeline-jump-go').addEventListener('click',()=>{const selectedYear=Number(year.value),monthValue=Number(month.value);if(!Number.isInteger(selectedYear)||selectedYear<1900||selectedYear>2300){toast('Informe um ano entre 1900 e 2300.','error');return}closeTimelineJump();goToTimelineDate(new Date(selectedYear,monthValue,1,12),true)});
    $('timeline-pdf-btn').addEventListener('click',openTimelinePdf);$('timeline-pdf-close').addEventListener('click',closeTimelinePdf);$('timeline-pdf-cancel').addEventListener('click',closeTimelinePdf);$('timeline-pdf-toggle-all').addEventListener('click',()=>{const boxes=[...$('timeline-pdf-list').querySelectorAll('input')],all=boxes.length&&boxes.every(box=>box.checked);boxes.forEach(box=>box.checked=!all)});$('timeline-pdf-preview-btn').addEventListener('click',()=>createTimelinePdf('preview'));$('timeline-pdf-download-btn').addEventListener('click',()=>createTimelinePdf('download'));$('timeline-pdf-backdrop').addEventListener('click',event=>{if(event.target===$('timeline-pdf-backdrop'))closeTimelinePdf()});$('obras-pdf-preview-close').addEventListener('click',closeObrasPdfPreview);$('obras-pdf-preview-cancel').addEventListener('click',closeObrasPdfPreview);$('obras-pdf-preview-backdrop').addEventListener('click',event=>{if(event.target===$('obras-pdf-preview-backdrop'))closeObrasPdfPreview()});$('obras-pdf-save').addEventListener('click',()=>obrasPdfPreviewDoc?.save(obrasPdfPreviewName));
    document.addEventListener('click',event=>{if(!event.target.closest('#timeline-month-toggle,#timeline-month-menu'))closeMonthMenu();if(!$('timeline-jump-popover').hidden&&!event.target.closest('#timeline-jump-popover,#timeline-jump-btn'))closeTimelineJump()});
    document.addEventListener('keydown',handleTimelineHistoryShortcut);
    window.__obrasSetTimelineMonth=setMonth;window.__obrasCloseTimelineMonth=closeMonthMenu;
  }
  async function boot(){try{state.client=SuperAppAuth.getScopedClient('core');state.profile=await SuperAppAuth.getProfile();profileCopy();configureTheme();configureShell();configureTimelineV25();await Promise.all([loadLocations(),loadWorks()]);$('app-shell').setAttribute('aria-busy','false');SuperAppAuth.releaseAppGuard()}catch(error){toast(safeMessage(error,'Não foi possível iniciar o acompanhamento de obras.'),'error');SuperAppAuth.releaseAppGuard()}}
  window.addEventListener('superapp:authorized',boot,{once:true});
} bootObras();
